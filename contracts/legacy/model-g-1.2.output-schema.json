{
  "type": "object",
  "properties": {
    "schema_version": { "type": "string", "enum": ["sp.visual-reference-receipt/1.0"] },
    "description_sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "status": { "type": "string", "enum": ["generated", "unavailable"] }
  },
  "required": ["schema_version", "description_sha256", "status"],
  "additionalProperties": false
}
