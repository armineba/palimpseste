# Capture du lecteur propriétaire reçue par l'API — 20 septembre 2026

Le nouveau lecteur `game/Build/WindowsPlayerLavaReady/Palimpseste.exe` a été lancé en fenêtre visible sur ce PC. Le processus PID 13764 appartenait à `DESKTOP-457AH3U\Utilisateur` et répondait. Son écran indiquait « Capture reçue / En file d’attente » pour un parchemin déjà engagé. L'ancien lecteur restait aussi ouvert ; aucun de ces deux processus n'a été pris pour un worker.

La capture d'écran privée `E:\Palimpseste\.runtime\operator-staging\player-lava-ready-20260920.png` a pour SHA-256 `5EDD811717AA62CF492E4F48F823ED1D02126892E17D0B521200384EB5C0023F` ; elle ne montre ni jeton ni secret et n'est pas publiée dans le dépôt.

Une lecture de `palimpseste_lab` a trouvé **un** job `production` en état `queued`, associé à l'identité propriétaire créée par l'invitation locale et à un parchemin `processing`. La capture a été créée le 20 septembre 2026 à 06:17:39 heure de Paris. Les fichiers du build LavaReady portent des heures de modification autour de 06:35 ce jour-là : cette version du Player a donc **repris et affiché** un job déjà reçu, sans preuve qu'elle ait transmis ce dessin. Son manifeste a pour SHA-256 `120312764D662140DB19AEC21FE8EB3611239E22EF5AF2A635A8B2A2278B7A77`. Les quatre artefacts en base sont présents : référence, dessin, encre et journal. Le dessin PNG serveur a pour SHA-256 `757B2284EDB0FE60D57CC1B7EE59511F4A9FF7F8AFE99196E4E5500A9BB91CCE` et l'encre `A97BF8F95BBC8DADA96CF32BCF1F46D90DBB11170367B2216AFD3A851FC590B7` ; les octets restent privés. L'image a été inspectée : une trace rouge-brun courbe traversant le noyau et la couronne, différente du trait droit utilisé pour calibrer A `1.3`.

`ops/status-lab.ps1` a rapporté `jobs|queued|1`, aucun `provider_attempts`, et les quatre types d'artefacts. Aucun appel Luna n'a été exécuté pour ce job, et aucun texte A, plan B ou sort publié n'est disponible. La file restera en attente tant que le worker joueur n'est pas admis et démarré.
