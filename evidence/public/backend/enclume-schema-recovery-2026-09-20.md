# Reprise du parchemin « Enclume filante » — 20 septembre 2026

Le job joueur `e3537609-1196-47d6-8750-fe79124f1a0a` avait une interprétation Astra A réussie, mais aucun plan Luna B. Son premier appel B s'est terminé par `provider_b_processfailure` : le fournisseur a répondu HTTP 400 `invalid_json_schema`. Dans le schéma structuré B, les six objets `appearance` déclaraient `palette` dans `properties` sans l'inclure dans `required`. Le dessin n'était pas en cause. L'appel B n'a pas produit de plan et aucun sort n'était alors disponible.

Les six `palette` sont désormais obligatoires dans le schéma envoyé à Codex, avec une valeur `null` permise pour les anciennes descriptions. Le prompt B `1.4` émet la clé. Le test de régression Core vérifie récursivement que chaque objet du schéma B a exactement ses propriétés dans `required`, et qu'un ancien plan à palettes nulles reste compilable. `dotnet run` sur `Palimpseste.Core.Smoke` a terminé avec le code 0. Le worker publié et déployé a pour SHA-256 `3C1FD75ABA28AE43A59915F280687B61D34BEB96BDBE9ED9E2246DA35FA199BB`.

Après déploiement, une transaction ciblée a vérifié l'identifiant, l'état, le code d'erreur, l'absence de bail, de plan et de tentative incertaine, ainsi que la présence de l'interprétation A réussie. Elle a remis **une** tâche et **un** parchemin en traitement sans effacer l'historique. Le worker a réutilisé la description et les géométries du dessin. La base indique ensuite `ready`, un plan B et un sort `43e21914-8f59-4fcd-803c-27385e530fd1`. Les tentatives sont A `success` (`gpt-6-astra/max` demandé), B initial `invalid`, B repris `success` (`gpt-5.6-luna/max` demandé). SHA-256 du plan `d141f261d723c71a967981101beb8cb75640b338d2e676ee8f3f9c2fec96f797`; SHA-256 du paquet compilé `46ac68ed274e13d0f12cc1fe3bfe9e2a36b61fed50268c86ca0a378fabd4f9a6`.

La nouvelle API locale a pour SHA-256 `F2DFF610776A67B07228CD345E2DBDE70B8D9616AAEBA7ECE29283E937193FE3`; `/health/ready` répond `200`. Elle limite la reprise par le propriétaire du même job aux échecs B éligibles et bornés, avec interprétation A persistée et sans tentative incertaine. Le nouveau client affiche « Réessayer ce dessin » quand l'API annonce cette possibilité. Pour ce job déjà repris, « Actualiser » doit ouvrir la fiche prête. Aucun lancement manuel de ce nouveau sort dans le laboratoire n'est revendiqué par ce rapport.
