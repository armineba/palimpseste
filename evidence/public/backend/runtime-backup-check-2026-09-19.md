# Essai du script de sauvegarde runtime — 19 septembre 2026

`ops/backup-runtime.ps1` a été exécuté sur la base synthétique `backup-source` et ses artefacts privés, avec PostgreSQL 17. La destination privée est `C:\ProgramData\Palimpseste\backups\runtime-backup-check-20260919T182520Z`. La commande a terminé sans erreur, a écrit un dump au SHA-256 `37b29b33ae40b2b011ab9f44843be38bfad079835ed8bd808877123f04a52e8c` et a manifesté **10 artefacts**. Un contrôle indépendant des fichiers copiés a retrouvé **10/10 empreintes SHA-256** conformes au manifeste. `icacls` sur le répertoire de sauvegarde ne montrait que l'utilisateur courant, Administrateurs et SYSTEM en contrôle complet.

La chaîne de connexion n'a été lue que depuis le fichier privé `C:\ProgramData\Palimpseste\backup-source-db.env`; elle n'est pas enregistrée ici. Le script transmet host, port et utilisateur via les variables libpq `PGHOST`, `PGPORT` et `PGUSER`, passe seulement le nom de base en argument, place temporairement le mot de passe dans `PGPASSWORD`, puis restaure ces variables. Ce test ne couvre pas encore `ops/restore-runtime.ps1` ni une sauvegarde pendant des écritures actives. La restauration à blanc déjà exécutée avec les scripts du laboratoire est documentée dans [api-restauration-2026-09-19.md](api-restauration-2026-09-19.md).
