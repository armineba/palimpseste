# Isolation observée du worker — 19 septembre 2026

Le compte Windows local `PalRuntimeSvc` a été créé pour le worker ; `net localgroup Administrateurs` a confirmé qu'il n'appartient pas au groupe des administrateurs. L'ACL du dépôt de développement contient pour ce compte un refus héritable `(RD,REA,X,RA)`. L'ACL de `E:\PalimpsesteRuntime\spec` lui donne `(RX)` ; les fichiers de la spécification ont été contrôlés en lecture seule. Le prompt de développement `00_AGENT_BUILD.md` n'est plus dans cette copie runtime ; seuls les prompts A, B et réparation y sont destinés.

Le [doctor local](provider-local-2026-09-19.json), exécuté sous `PalRuntimeSvc`, a confirmé que les fonctions d'outils exposées demandées sont observées désactivées dans `codex features list`. Le mécanisme interne `unified_exec` reste indiqué actif par le CLI ; la [documentation de configuration de Codex](https://developers.openai.com/fr-FR/docs/config-file/config-basic) le décrit comme une implémentation PTY, tandis que `shell_tool` active l'outil shell et est observé `false`. La preuve locale complète est hashée hors Git et référencée par `runtime.env`. Le dernier doctor ne rapporte que `effort_not_verified` parmi les blocages de production.

Ces observations de configuration et d'ACL ne sont pas un test adversarial de l'exécution d'un modèle. L'authentification dédiée Codex manque ; le [doctor actif final](provider-active-blocked-2026-09-19.json) s'est arrêté avec `dedicated_auth_not_confirmed` et `model_calls_executed=false`. Le worker refuse donc encore les jobs de génération. Les captures joueur, paquets et secrets ne sont pas copiés dans ce rapport public.
