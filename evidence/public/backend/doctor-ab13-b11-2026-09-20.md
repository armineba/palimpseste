# Diagnostic actif A `1.3` / B `1.1` — 20 septembre 2026

Une sonde réelle sous `PalRuntimeSvc` a utilisé les deux images de conception du même dessin avec `gpt-5.6-luna` à l'effort `max`. Le rapport complet reste privé dans `E:\PalimpsesteRuntime\evidence\pending\doctor-active-ab13-b11-20260920.json`. Son SHA-256 vérifié est `256E564490E1A0A7E5BC50FBA216F4DC29F40EF496C867E03F3BE14291349610`. Les sorties finales, images et journaux privés ne sont pas copiés dans le dépôt.

| Étape | Modèle / effort rapportés | SHA-256 de la sortie finale | Observation |
| --- | --- | --- | --- |
| A multimodale `1.3` | `gpt-5.6-luna` / `max` | `E54DEC5810E00AABD0FFCB8F152DF06468DD8505593B6718C5B806C46B3254B4` | Titre « Faisceau de lave » ; résumé sans effet ni cible. |
| B traductrice `1.1` | `gpt-5.6-luna` / `max` | `D7ED136F345CFFB0DA51C25AA6A7EE198F8B034073BA8EF997BCF39E65571E58` | Faisceau visuel avec filtre technique `environment`, `lifetime_ticks=1`, `tick_interval=1`, `effects=[]`. |

La lecture et le plan ont passé le contrôle structurel préalable. En revanche, `Palimpseste.Core.RealProbe` a rejeté ce couple avec `signature_geometry` : le nœud `n0` demandait une silhouette existante, alors que le doctor avait reçu un `geometry.json` provisoire, non recalculé depuis cette nouvelle description A. Ce diagnostic ne prouve donc **aucune compilation contrôlée réussie** de ce plan. Il faut recalculer les géométries à partir de la sortie A et de l'encre réelle, puis refaire la validation et la compilation sans nouvel appel modèle si les sorties restent figées.

La [migration 006 du laboratoire](migration006-owner-lab-2026-09-20.md) a préparé l'enregistrement de la version du prompt B. Elle ne résout pas le refus géométrique et ne constitue pas une preuve de sort joueur.

Le worker joueur reste arrêté et `PALIMPSESTE_EFFORT_VERIFIED=false`. Aucun paquet issu de cette sonde n'a été publié, téléchargé ou lancé dans le Player Unity. Aucun verdict humain d'acceptation de cette lecture ou du sort n'est enregistré ; M7 reste ouvert.
