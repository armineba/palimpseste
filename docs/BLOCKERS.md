# Blocages et portes ouvertes

Mis à jour le 20 septembre 2026.

| Sujet | État observé | Prochaine action |
|---|---|---|
| Lecture du dessin par Luna | Le prompt A `1.3` a réellement lu le trait rouge-brun comme un faisceau de lave unidirectionnel, sans cible ni dégâts. Le modèle `gpt-5.6-luna` et l'effort `max` ont été rapportés. Le contrôle structurel D01 passe ; l'ancienne A `1.2` y échoue sur des dégâts inventés. | L'auteur doit juger la nouvelle description et le rendu. Son verdict sur A `1.1` était négatif ; aucun accord sur A `1.3` n'est enregistré. |
| Traduction et compilation | B `1.1` a été rappelé seul sur la A figée, avec la géométrie réellement extraite de l'encre ; modèle et effort `gpt-5.6-luna`/`max` rapportés. Le validateur et `RealProbe` ont accepté le paquet de test. Le nouveau rendu suit le chemin de pixels de la silhouette : tests de géométrie ciblés 2/2, puis tests du paquet Luna réel en Direct3D 12 2/2, 111 points source dont 94 visibles, 36 440 pixels rouges puis zéro après nettoyage, sans dégât. Ce paquet conserve des identifiants d'artefacts de sonde et n'est pas publié au joueur. | Exécuter un vrai job joueur de bout en bout, téléchargement, lancement dans le Player IL2CPP et reprise hors ligne, puis recueillir le verdict humain. |
| Worker joueur | Compte `PalRuntimeSvc` authentifié, migration 006 appliquée aux bases laboratoire et test. Le worker FINAL (76 231 207 octets, SHA-256 `987946213762B3E4052D1218377F3CFA748EF3F42BD7FF935E52BBE48CFF2F4A`) et son enfant SHA-256 `D776764F843BC2B2BD2D6A39FAFB6D3B77006C92ECCAA915DC8909E4B124CCC8` sont candidats, non installés ; aucun worker ne tourne. Le doctor FINAL SHA-256 `2F323942747724AAC599052854947C8C4341A561BD40F075F15989A24B7E527A` est installé. Son mode `validate` sous service sur les fichiers A/B réels et l'encre a quitté avec le code 0 : validation et compilation hors ligne réussies, aucun appel modèle, rapport privé SHA-256 `D78451754EEBDC32624E4EF4771D6928CBC0D6A8FD162909B5208B047E35FEFA`. Les tests de sécurité synthétiques ont quitté avec le code 0, sans nouvel appel Luna. L'ancien rapport `active` est refusé seul ; la preuve composite v2 exige trois rapports et les fichiers A/B/encre originaux. `PALIMPSESTE_EFFORT_VERIFIED=false`. Voir `evidence/public/backend/composite-worker-prep-2026-09-20.md`. | Après verdict humain sur A `1.3`, revoir les trois rapports et les hashes des fichiers A/B/encre, préparer le manifeste composite v2 approuvé, puis suivre `docs/ops/OWNER_WORKER_CUTOVER.md` pour l'installation contrôlée. Ne pas marquer un sort prêt avant publication réelle. |
| Laboratoire sur ce PC | API sous `PalRuntimeSvc` sur `127.0.0.1:18080`, `/health/ready` 200 ; Player BeamGeometryReady visible et répondant (PID 35552 au contrôle). Il a repris une vraie capture rouge-brun courbe déjà reçue : un job `production` reste `queued`, avec référence, dessin, encre et journal. Aucun A/B n'a traité ce job. Le retour d'interprétation a passé 9/9 contrôles HTTP/DB sur données synthétiques. | Après le verdict sur A `1.3` et l'admission du worker, traiter ce job sans refaire le dessin, puis vérifier l'écran de texte et le laboratoire sur ce même job. |
| Packaging et revue automatique | Le démarrage initial de l'API qui plaçait un environnement privé dans l'appel de l'outil a été refusé avant exécution avec `blocked by policy`, sans motif détaillé. Un lanceur enfant qui lit les secrets sous le compte de service a résolu ce démarrage, vérifié par HTTP et PID. La création du ZIP du Player courant a subi un refus distinct avant exécution ; le dossier exécutable existe. | Livrer et tester le dossier Windows actuel. Ne pas contourner le refus de ZIP par une commande équivalente ; conserver ce refus dans la preuve. |
| Ouverture à des joueurs externes sur un abonnement personnel commun | La [documentation officielle Codex](https://developers.openai.com/fr-FR/docs/auth) distingue connexion ChatGPT par abonnement et clé API facturée à l'usage, et déconseille d'exposer l'exécution Codex dans un environnement public ou non fiable. Les [conditions européennes](https://openai.com/fr-FR/policies/eu-terms-of-use/) interdisent de rendre un compte individuel accessible à autrui. L'application de ces textes à une passerelle de jeu qui masque les identifiants demande une confirmation explicite ; il s'agit d'une inférence, pas d'un avis juridique. Aucun endpoint public n'est ouvert. | Obtenir une confirmation écrite d'OpenAI ou un accord adapté avant d'ouvrir la passerelle à des tiers. Continuer les essais propriétaire locaux sans API payante ni achat déclenché par le code. |
| Coût du compte | Les sondes ont réellement consommé l'usage du compte connecté. Le code ne lance aucun achat ou rechargement. La capture de réglage envoyée par l'utilisateur ne suffit pas à vérifier un verrou de facturation du compte au moment des appels. | Contrôler le réglage de recharge et le solde dans l'interface du compte avant toute ouverture ou campagne de 30 dessins ; à quota épuisé, laisser le worker en attente. |
| Recette M7 | Ni les 30 dessins inédits, ni le second créateur, ni la machine Windows propre, ni le verdict humain signé ne sont réalisés. | Conduire la recette sur le build et les prompts figés ; conserver chaque sortie et sa décision humaine. |

Les preuves de diagnostic, les paquets de sonde et les tests isolés ne valent pas un parcours joueur publié ni une acceptation humaine.
