# Blocages et portes ouvertes

Mis à jour le 20 septembre 2026.

## État courant D13 — essai local disponible

Le backend D13 est déployé, `/health/ready` est prêt et le Player `1.3.0` est ouvert, PID `39012`, fenêtre réactive. [Preuve de déploiement](../evidence/public/backend/image-reference-deployment-2026-09-20.json). Les échecs du préflight PowerShell et de l'écriture du manifeste ont été corrigés ; ils ne bloquent plus cet essai.

- **À constater dans le jeu :** nouveau dessin → description → vraie image → construction → lancement ; aucun parcours joueur D13 complet encore enregistré.
- **À juger par le créateur :** fidélité à l'image, beauté, mouvement et son. Capture Unity 04 réussie, mais dernier réglage du verre non recapturé ; aucune acceptation artistique présumée.
- **Hors preuve actuelle :** relecture hors ligne du nouveau parcours D13, recette des 30 dessins, second créateur, autre machine et accès distant/multijoueur.

Les quatre appels de diagnostic A/G/B1/B2 sont conservés ; B1 rejeté, B2 validé et compilé avec le même PNG. Aucun achat/rechargement n'est déclenché par le code. [Instructions de test](TESTER_MAINTENANT.md).

## Historique — incidents et portes des premières versions

| Sujet | État observé | Prochaine action |
|---|---|---|
| Nouveau dessin en attente | Le Player PID `35804` lancé sans API a conservé le second dessin en `capture_pending` avec `needs_begin=true`, `needs_capture=true` et 151 entrées de journal. L'API répondait ensuite 200 et le worker tournait, mais aucun nouveau job n'existait. Le bouton « Reconnecter » est codé ; `WindowsPlayerReconnectPlayable/` est produit, sans lancement du nouveau Player ni reprise attestée. Voir la [preuve de l'incident](../evidence/public/unity/player-reconnect-pending-capture-2026-09-20.md). | Sur le même profil Windows, ouvrir ce parchemin avec le nouveau build, cliquer « Reconnecter », puis vérifier la création du job et l'affichage de A. Relever l'erreur si la reprise échoue ; le dessin peut alors être refait. |
| Lecture du dessin par Luna | Le prompt A `1.3` a réellement lu le trait rouge-brun comme un faisceau de lave unidirectionnel, sans cible ni dégâts. Le modèle `gpt-5.6-luna` et l'effort `max` ont été rapportés. Le contrôle structurel D01 passe ; l'ancienne A `1.2` y échoue sur des dégâts inventés. | L'auteur doit juger la nouvelle description et le rendu. Son verdict sur A `1.1` était négatif ; aucun accord sur A `1.3` n'est enregistré. |
| Traduction et compilation | Le B `1.1` de calibration sur A figée et géométrie extraite de l'encre avait passé `RealProbe` et Direct3D 12 2/2. Le **job joueur distinct** a ensuite terminé A/B avec deux tentatives fournisseur réussies, une description, un plan et un `compiled_spell`, sans tentative en échec. Le Player a téléchargé et vérifié son sort ; son compteur a affiché `Lancers : 1`, `Dégâts : 0`. Le paquet joueur a passé PlayMode 2/2 avec le correctif de visibilité. | Recueillir les verdicts humains sur la lecture, le rendu, le son et l'intérêt du sort. |
| Worker joueur | `PalRuntimeSvc` authentifié et migration 006 appliquée. Worker installé SHA-256 `9243F9600973B1AAF5F97F07AD0983FF60A7EFA2EB0A7F7D42D142C05015847D`, enfant SHA-256 `03CE0108D67A4AC27B332A41F36F1F12B2B362FA9A2AEBCF25EB21E8BDB4C104`, PID `8180` observé. Doctor installé SHA-256 `AAAAB2686CD9A33ADB6130B205D10596A63AF8207CBAE580ACD723C862196811`, diagnostic local sans `production_issues`. La preuve composite v2 lie trois rapports et les A/B/encre originaux ; manifeste SHA-256 `56F349016A5B63D352DD37225AF9253889D496C2399F58F97F70D0DAAC353E3F`, `PALIMPSESTE_EFFORT_VERIFIED=true`. L'ancien rapport actif seul reste refusé. Voir `docs/ops/OWNER_WORKER_CUTOVER.md`. | Conserver les fichiers et ACL des preuves ; surveiller les futurs jobs sans reprise aveugle d'une tentative fournisseur incertaine. La preuve technique ne signe pas le verdict artistique. |
| Laboratoire sur ce PC | API redémarrée sous `PalRuntimeSvc`, PID `16420`, `127.0.0.1:18080/health/ready` 200 ; worker PID `8180`. Player `WindowsPlayerBeamVisibleReady` construit par Unity (code 0), 29 fichiers et 122 093 149 octets, lancé sous PID `35804`. Le job courbe est `ready` ; texte A « Faisceau courbe de lave », fiche, laboratoire, lancement sans dégâts. Une capture montre l'effet visible puis disparu. API précédente arrêtée, Player relancé : bibliothèque, fiche, labo et lancement du même sort ont fonctionné sans API ; API ensuite redémarrée. | Faire écouter le son à un créateur et consigner ses verdicts. Ne pas présenter ce seul dessin comme la recette des 30 cas. |
| Packaging et revue automatique | Le démarrage initial de l'API qui plaçait un environnement privé dans l'appel de l'outil a été refusé avant exécution avec `blocked by policy`, sans motif détaillé. Un lanceur enfant qui lit les secrets sous le compte de service a résolu ce démarrage, vérifié par HTTP et PID. La création du ZIP du Player courant a subi un refus distinct avant exécution ; le dossier exécutable existe. Après la relecture sans API et le redémarrage de l'API, une commande de confort pour fermer puis relancer le Player en ligne a aussi été refusée avant exécution ; aucun essai équivalent n'a suivi. Le Player PID `35804` reste ouvert dans la session de relecture sans API, l'API PID `16420` est prête et le worker PID `8180` actif. | Livrer le dossier Windows actuel et conserver les refus dans la preuve. Ne pas présenter une relance du Player après restauration de l'API comme testée. |
| Ouverture à des joueurs externes sur un abonnement personnel commun | La [documentation officielle Codex](https://developers.openai.com/fr-FR/docs/auth) distingue connexion ChatGPT par abonnement et clé API facturée à l'usage, et déconseille d'exposer l'exécution Codex dans un environnement public ou non fiable. Les [conditions européennes](https://openai.com/fr-FR/policies/eu-terms-of-use/) interdisent de rendre un compte individuel accessible à autrui. L'application de ces textes à une passerelle de jeu qui masque les identifiants demande une confirmation explicite ; il s'agit d'une inférence, pas d'un avis juridique. Aucun endpoint public n'est ouvert. | Obtenir une confirmation écrite d'OpenAI ou un accord adapté avant d'ouvrir la passerelle à des tiers. Continuer les essais propriétaire locaux sans API payante ni achat déclenché par le code. |
| Coût du compte | Les sondes ont réellement consommé l'usage du compte connecté. Le code ne lance aucun achat ou rechargement. La capture de réglage envoyée par l'utilisateur ne suffit pas à vérifier un verrou de facturation du compte au moment des appels. | Contrôler le réglage de recharge et le solde dans l'interface du compte avant toute ouverture ou campagne de 30 dessins ; à quota épuisé, laisser le worker en attente. |
| Recette M7 | Ni les 30 dessins inédits, ni le second créateur, ni la machine Windows propre, ni le verdict humain signé ne sont réalisés. | Conduire la recette sur le build et les prompts figés ; conserver chaque sortie et sa décision humaine. |

Le parcours joueur privé a produit et rejoué un sort sans API. Les preuves de diagnostic et ce parcours ne valent pas acceptation humaine ni ouverture publique.
