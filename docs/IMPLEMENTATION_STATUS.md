# État de réalisation et point de reprise

## D20 — Pipeline V2 construite, installation en attente

**Backend D20 et Player 1.8.0 compilés et empaquetés**, Unity 6.3 / URP / IL2CPP, sorties 0, sans erreur de shader. [Livraison](../evidence/public/backend/blueprint-v2-d20.json) · [Architecture, diagramme, types et gates](D20_SPELL_PIPELINE_V2.md) · [Fichiers](V2_FILES_CHANGED.md).

**27 contrôles V2 réussis**, compatibilité V1 vérifiée (141 recettes / 705 paires), 172 assets historiques inchangés dans Git. Fixture séparée réellement exécutée : continuité, quatre contacts, vraie caméra et vrai lancer ; boucle 4,5 s refermée avec écart 0 m. Le banc de capture magenta est corrigé. [Résultats](../evidence/public/v2/results.json) · [Vidéo](../evidence/public/v2/fixture/motion-real-time.mp4).

**Qualité artistique de la fixture refusée par la critique indépendante**, acceptation humaine absente. Les mesures GPU et d'overdraw sont inconnues.

**D20 non installé et nouveau parcours Codex non exécuté** : Windows a annulé la première élévation opérateur. Une question demandant d'accepter la prochaine fenêtre UAC est en attente. L'ancien service D19, son Player et la bibliothèque restent conservés. Nouveau Player séparé dans `game/Build/WindowsBlueprintV2Playable`. Voir la reprise détaillée D20 pour le diagnostic isolé puis le déploiement 012 ; ne pas prétendre que le jeu ouvert utilise déjà V2.

## Historique — D19 / Player 1.7.0 installé et ouvert

**Backend et Player compilés, empaquetés et installés ; jeu rouvert le 24 septembre à 23:20:41 Paris**, PID **33920**. Quatre profils par partie sont exécutés dans le shader : plasma, force_field, toxic et spectral_flow. Ils portent des calculs TinyPlay MIT et Keijiro Unlicense, avec deux textures TinyPlay originales échantillonnées en RGB. B **2.6** reçoit leurs capacités dans les nouveaux dossiers de recherche ; les paquets les utilisant exigent le client 1.7.0. [Provenance](D19_SOURCED_SURFACES.md) · [Preuves](../evidence/public/backend/sourced-surfaces-d19.json) · [Installation](../evidence/public/backend/sourced-surfaces-d19-installation.json).

Build Unity final : sortie **0**, sans erreur de shader ; Player livré en **34 fichiers / 132 056 125 octets**, ZIP de **46 852 159 octets**. La première tentative Unity comportait une erreur HLSL malgré son résultat Success : elle est conservée comme tentative rejetée, corrigée puis reconstruite. Compilation backend finale : **0 erreur / 0 avertissement** ; première tentative en erreur sur une variable locale dupliquée, corrigée. Aucun test, appel modèle de diagnostic, capture ou essai de jeu exécuté par l'agent.

La génération déjà en cours s'est terminée avant la bascule. Suspension des prises de jobs retirée ; concurrence toujours **sans plafond applicatif**, captures GPU à tour de rôle. Les anciens paquets restent identiques. **Fidélité visuelle et acceptation humaine D19 encore ouvertes** : le créateur essaie un nouveau parchemin dans le jeu rouvert. [Point de reprise](NEXT_ACTIONS.md).

## Historique — D18 déployé, jobs sans plafond applicatif

**Backend D18 publié et installé le 24 septembre à 22:52:32 Paris**, avec `PALIMPSESTE_MAX_PROVIDER_CONCURRENCY=0`. Un seul worker traite plusieurs jobs indépendants ; chacun garde son bail et ses checkpoints. Les captures Unity passent à tour de rôle sur le GPU, sans bloquer les autres étapes des autres parchemins. Le Player **1.6.0** reste celui déjà livré. [Architecture](D18_CONCURRENT_JOBS.md) · [Preuve](../evidence/public/backend/parallel-jobs-d18.json) · [Installation](../evidence/public/backend/parallel-jobs-d18-installation.json).

Compilation Release : code **0**, **0 erreur / 0 avertissement**. Publication : code **0**. Installation : `completed=true`, `services_started`, pause temporaire de claims retirée. Aucun test ni génération indépendante exécuté. La consultation supplémentaire de la file a été refusée par le contrôle automatique, sans motif détaillé ; la concurrence effective des appels natifs reste à observer sur les essais du créateur. Le déploiement ne prouve ni gain de latence mesuré ni acceptation artistique. Voir [le point de reprise](NEXT_ACTIONS.md).

## Historique — D17 déployé, bibliothèques avant reconstruction

Les cinq sources imposées par le créateur sont documentées et deviennent obligatoires dans les nouveaux dossiers de recherche entre la planche et B. Le prompt B **2.5** utilise leurs techniques pertinentes pour reconstruire les phases de la planche, puis reprendre les corrections Dream-loop Pro. Les recherches persistées restent immuables. [Consigne](BIBLIOTHEQUES_VFX_OBLIGATOIRES.txt) · [Inventaire](../reference/vfx-libraries.inventory.json).

Worker compilé en Release avec code 0, sans avertissement ni erreur : [journal](../deliverables/vfx-library-d17-build.log). **Publication backend code 0 et installation achevée le 24 septembre à 21:59:55 Paris.** Prompt B **2.5**, resolver et index des cinq sources obligatoires sont installés. Worker PID **25352**, API PID **32092**, disponibilité HTTP **200** constatée à **22:00:32 Paris** ; Player 1.6.0 inchangé, rouvert PID **32492**. [Preuve d'installation](../evidence/public/backend/vfx-libraries-d17-installation.json).

**Packages non importés dans le Player ; aucun test, appel modèle ou capture effectué pour cette livraison.** Les quatre clones de consultation ne constituent pas de nouveaux assets exécutables. La suppression des six parchemins locaux demandée par le créateur a été rejetée par le contrôle automatique (`blocked by policy`, sans motif détaillé) ; ils restent présents, dont un dessin en attente sans job serveur associé. Aucune sauvegarde créée ni suppression serveur effectuée. Voir [la reprise](NEXT_ACTIONS.md) pour le travail restant et les limites observées.

## Historique — correctif backend D16.1, Player 1.6.0 conservé

Le dernier parcours joueur « Foudre sous l’Enclume » a produit sa description, sa planche 3 × 7, trois plans et deux verdicts admissibles. Le troisième J avait renvoyé un hash de plan erroné, bloquant la livraison. Le backend corrige cette cause par des empreintes imposées dans le schéma J, des diagnostics précis et une finalisation depuis le meilleur candidat déjà évalué en cas de J définitivement invalide.

**Publication et installation réussies avec le code 0.** La reprise ciblée du job est **ready à 07:33:56 Paris le 21 septembre**, avec **8 appels avant et après**, deux critiques admises et trois dossiers de capture avant et après. Révision 1 sélectionnée, score réel **4190/10000**, cycle encore jugé non fidèle. Aucun nouveau modèle, capture ou test exécuté pour cette reprise ; aucun rebuild Unity. [Preuve D16.1](../evidence/public/backend/visual-judge-fix-d16-1.json) · [Explication](D16_JUDGE_RECOVERY.md) · [Point de reprise](NEXT_ACTIONS.md).

Le joueur peut actualiser son parchemin et lancer le sort disponible. Le rendu reste à apprécier humainement ; les prochains appels J avec schéma fixé restent à observer. Le ZIP backend courant est indexé dans [la livraison](../evidence/public/backend/lifecycle-delivery.json).

## Historique — D16 / 1.6.0 construit et installé

Le format demandé est implémenté : titre, fond sombre, trois lignes APPARITION / STABLE / DISPARITION et sept cases numérotées. La description reste l’autorité du comportement, la planche guide sa réalisation visuelle. Le PNG natif est conservé avant la mise en page fixe, avec un SHA distinct. B construit un seul sort animé ; J compare sept poses réelles de chaque phase dans la boucle de génération joueur.

**Player et backend 1.6.0 construits, empaquetés et installés avec le code 0**. Unity 6000.3.24f1 / URP / IL2CPP ; ZIP Player de **46 591 080 octets**, SHA `24ca0f949892fd3017889e8c0a1f23c48cd69f63125174a36951525739c5507f`. Migration 011 appliquée, renderer protégé installé, API **6640** et worker **30380** démarrés le 21 septembre à 01:48 Paris. Raccourci Bureau actualisé. [Preuve D16](../evidence/public/backend/animation-sheet-d16.json) · [Manifeste Player](../evidence/public/unity/lifecycle-delivery.json) · [Archive backend](../evidence/public/backend/lifecycle-delivery.json).

**Aucun essai de jeu, test, appel modèle ou capture indépendante exécuté pour D16.** La planche générée, le parcours complet, la fluidité, la latence et la fidélité restent à observer par le créateur. La compilation et le démarrage du service ne valent pas acceptation artistique. [Point de reprise](NEXT_ACTIONS.md) · [Architecture D16](D16_ANIMATION_SHEET.md).

## Historique — D15 / 1.5.0 construit et installé

**La chaîne des prochains parchemins est mise à jour**, sans réécrire les sorts ou documents existants. Placement, trajectoire et phénomène sont déclarés par A, repris par B et contrôlés avant exécution. Le moteur applique rotation continue, écoulement, orbite, turbulence et balistique. Une recherche de références précède chaque construction ; 16 textures CC0 et du bruit Simplex MIT sont effectivement utilisés. La critique Pro dispose de quatre instants actifs.

Player Unity 6000.3.24f1 / IL2CPP : **build et packaging code 0**, version 1.5.0, ZIP **46 574 212 octets**, SHA `e8669bf3ffbcc77134186d2b1d6ed76702de9dac666e60a7d85c5d6a58e647b1`. Backend publié et **installé code 0**, migration 010 appliquée le 21 septembre à 01:17 Paris ; worker PID 38748 et API PID 37400 prêts au démarrage. Le raccourci Bureau pointe sur `WindowsBehaviorPlayable`. [Preuve D15](../evidence/public/backend/behavior-d15.json) · [Player et fichiers](../evidence/public/unity/lifecycle-delivery.json).

**Aucun essai de jeu, appel de génération, capture indépendante ni campagne de tests exécuté pour D15.** La recherche par job et la boucle Pro modifiées sont installées mais n'ont pas encore été exercées sur un nouveau parchemin par l'agent. Interprétation, comportement physique, fluidité, latence et fidélité artistique restent à valider par le créateur. [Essayer](TESTER_MAINTENANT.md) · [Architecture et limites](D15_BEHAVIOR_AND_RESEARCH.md).

## Historique — correctif D14 / 1.4.1 livré, reprise du sort en affinage

Le job joueur réel `7ed4561feffd43ebbafe48d9b960716d` a réussi **A, G et B**, puis s'est arrêté après l'image. Le processus de capture Unity a quitté avec le code 0, mais ses **quatre PNG sont noirs et identiques**. Son manifeste rapporte **11915,2178 FPS**, au-dessus de la borne `10000` du lecteur de capture ; le rejet est remonté sous le message générique `worker_exception`. Une sortie de processus réussie ne prouve donc pas qu'une image du sort a été rendue.

Ces constats viennent de **l'essai demandé et lancé par le créateur**, pas d'une campagne de validation indépendante. Le correctif `1.4.1` utilise un rendu URP explicite, identifie les erreurs de capture et permet une reprise authentifiée par le propriétaire. **Build, packaging et déploiement ont réussi avec le code 0.** Le [manifeste Player](../evidence/public/unity/lifecycle-delivery.json) atteste 29 fichiers, 122 861 334 octets, ZIP 44 367 955 octets, SHA-256 `15e056a3e21ae1549c3af857ca03cbdaf3a1394b4e0369511c9da77ce3489416`. Le Player corrigé est ouvert, PID `2140` ; API `25220`, worker `21488`, renderer protégé déployé. [Preuve du correctif et de la reprise](../evidence/public/backend/lifecycle-capture-fix-2026-09-20.json).

Seul le job bloqué a été repris après vérification de ses documents et de son verrou : une ligne réadmise en `refining_visuals`, entrée initiale conservée, **aucun nouvel A, G ou B initial**. La capture runtime suivante est terminée : quatre phases 1024 × 1024 aux SHA distincts, 59 rendus caméra achevés. La mesure est **71,1397 rendus/s**, sur 54 images et 0,7590697 s, avec lecture GPU synchrone ; elle exclut démarrage/encodage PNG et ne mesure pas les FPS de gameplay. Les images joueur ne sont pas publiées dans les preuves.

**État observé le 20 septembre 2026 à 23:28:06, heure de Paris :** J a réellement réussi et donné **2250/10000**, `lifecycle_faithful=false` ; la correction B suivante est en cours, état `refining_visuals`, sans erreur. Le défaut de capture a donc été franchi, mais le sort n'est pas encore `ready` et sa fidélité reste insuffisante selon J. Aucun nouveau test ou diagnostic indépendant n'a été exécuté ; les captures et appels constatés appartiennent à la reprise du job joueur. L'achèvement, le rendu en laboratoire et le verdict humain restent ouverts.

## Historique — D14 / 1.4.0 déployé localement et ouvert pour le créateur

La description définit désormais **quatre phases par sujet : apparition, activité, réaction au contact et disparition naturelle sans contact**. L'image générée reste la cible du moment actif. A `sp.prompt.a/2.3` décrit ce cycle ; B `sp.prompt.b/2.2` le traduit en profils d'animation et de VFX bornés. Le renderer Unity, fixé et compilé par le développement, exécute ces données sans produire de code depuis les dessins.

La nouvelle boucle de revue associe le plan compilé à **quatre captures réelles du Player de rendu précompilé**, puis les transmet avec l'image cible à une **nouvelle session critique J**, prompt `sp.prompt.j/1.0`. B peut ensuite reprendre les seuls profils `construction`, `vfx` et `lifecycle` ; les mécaniques doivent rester identiques. La boucle est bornée, conserve les verdicts et choisit le meilleur candidat effectivement évalué. Cette implémentation ne garantit pas un score parfait ni une fidélité artistique acceptée. Les captures et appels de cette boucle sont des actions runtime prévues lors de la génération demandée par le joueur ; aucun modèle n'obtient de commande de capture, d'outil de code ou de build.

| Élément D14 | État effectivement établi |
| --- | --- |
| Sources | Contrats de cycle de vie, prompts A/B/G/J, profils du renderer, critique indépendante et persistance de revue implémentés. Migration `009_visual_review.sql` appliquée au runtime local. |
| Build Unity | **Player `1.4.0`, Unity 6000.3.24f1 / URP / Windows IL2CPP construit avec le code 0**. Journal local `game/Logs/lifecycle-build.log` : `Build Finished, Result: Success`, marqueur `PALIMPSESTE_BUILD_OK`, retour 0. Sortie `game/Build/WindowsLifecycleRelease/`. |
| CoreSmoke | Une exécution a réussi **avant** la dernière demande d'arrêter les tests. Ce résultat ne valide pas les ajouts ultérieurs de critique ou un parcours joueur D14. |
| Exécution D14 | **Aucun appel modèle, capture de validation, essai de jeu ni verdict visuel D14 effectué par le développement pour cette livraison.** Le créateur demande à tester lui-même ; ne pas relancer une campagne. |
| Packaging et lancement Player | Packaging réussi, code 0 : **29 fichiers, 122 857 102 octets**, ZIP **44 366 062 octets**, SHA-256 `98a3dd7315ea6f1c327ab7bba38b5464923e65ed19dd4a7ee4da8efe7895da36`. [Manifeste du packaging](../evidence/public/unity/lifecycle-delivery.json). Raccourci Bureau actualisé, puis **Player `1.4.0` ouvert sous le PID `36512`** depuis `WindowsLifecyclePlayable` ; ancien Player `1.3.0` fermé proprement. Le lancement distinct figure dans la [preuve D14](../evidence/public/backend/lifecycle-2026-09-20.json), sans essai de jeu. |
| Déploiement backend | **Réussi, code 0**, via `ops/deploy-lifecycle.ps1` : migration `009` appliquée, renderer précompilé installé avec protections, API PID `16768` et worker PID `26260` démarrés. Démarrage prêt ; aucun diagnostic, appel modèle ou essai de génération exécuté. [Preuve publique](../evidence/public/backend/lifecycle-2026-09-20.json). Archive backend publiée et actualisée : [empreinte finale](../evidence/public/backend/lifecycle-delivery.json). |
| Bibliothèque locale | **21 dossiers de sorts supprimés**, puis leur sauvegarde définitivement supprimée conformément à la demande du créateur. Bibliothèque vide lors de l'observation. Aucun historique serveur purgé. |

**Point de reprise : laisser le créateur dessiner et juger dans le jeu ouvert.** Le parcours A → G → B → captures → J → éventuelle reprise B, sa latence, la fluidité, les impacts, le son, la sauvegarde/relecture et la fidélité à l'image restent à observer dans cette version. Toutes les validations humaines demeurent ouvertes. Les preuves D13 ci-dessous sont historiques et ne valident pas D14.

## Historique — D13 livré localement, image générée puis reconstruction

**La nouvelle cible est dessin → description → vraie image générée → composition 3D/VFX depuis l'image → laboratoire.** [D13](DECISIONS.md) remplace l'interdiction D10–D12 d'une image générée par joueur. Les choix A/B restent ceux de D10 : Sol/high pour A, Astra/high pour B ; G ajoute Astra/high avec l'outil natif `image_generation` de Codex et l'abonnement connecté, sans clé API. L'audit a trouvé `apply_patch` encore exposé dans l'ancien CLI via `code_mode_only` malgré les flags désactivés. Le nouveau binaire `codex-image.exe` doit servir **A/B/G**, avec un filtre du registre natif : aucun outil A/B, seulement l'image native en G. L'ancien fichier `codex.exe` et ses preuves restent archivés ; de nouvelles preuves liées au SHA durci sont obligatoires. **D13 est désormais déployé sur le service local et le Player `1.3.0` est ouvert pour l'essai du créateur.**

| Élément D13 | État constaté au 20 septembre 2026 |
| --- | --- |
| Contrats et compilateur | Implémentés et compilés : `visual_reference`, hash du plan, `appearance.construction`, parties et mouvements bornés, client minimum `1.3.0`, compatibilité des anciens paquets. |
| CoreSmoke | Réussi, exit 0 : provenance image/description, construction obligatoire, champs stricts, bornes et budget après expansion, mécanique inchangée. Métadonnées synthétiques ; aucun modèle appelé. |
| Migration 008 et checkpoint G | Worker.DbSmoke réussi, exit 0, sur `palimpseste_test` avec fixtures synthétiques nettoyées. Image figée, fences, rollback et reprise après G/incidents B vérifiés. Migration 008 ensuite appliquée au runtime local lors du déploiement réussi. |
| Prompt B, renderer et build Unity | Prompt `sp.prompt.b/2.1` déployé : `turn_mdeg_s=0` hors `homing`, après le rejet réel B1. Build final Unity réussi avec le code 0 : [journal](../evidence/public/unity/image-reference/build-final.log). Capture réelle **04, 1/1 Passed** : impact 1, dégâts 12000 milli-unités, impulsion 1, zéro porteur/renderer restant, cache inchangé. [Preuve](../evidence/public/unity/image-reference-2026-09-20.md). Dernier réglage du verre après capture non recapturé ; acceptation artistique ouverte. |
| Validateur de paquets Unity | **4/4 tests Passed**, [preuve XML](../evidence/public/unity/image-reference/packet-validation.xml). Durée des tests : **0,265895 s**, sans inclure le démarrage/import Unity. Aucun appel modèle dans ces contrôles. |
| Transport natif A/B/G durci | Compilation native réussie, exit 0. SHA du candidat : `0a38e51ceca23710d2ced5ed06c6822584aa8a8e418defe116a994ce384d3a4f`. Diagnostic local réussi sans appel modèle ; les flags requis ont été observés désactivés. Les nouvelles sondes utilisent ce binaire, les anciens rapports ne le valident pas. |
| A et G réels | Sonde A Sol/high réussie en **34,091 s**, schéma valide. G Astra/high réussi en **55,784 s**, PNG réel **1536 × 1024**, SHA `04f621d5cc1b75cba6c7c564c0ffc4f77e944b82826000ad484643e0071f6bee`. G réutilise une description antérieure, distincte de la nouvelle sortie A. Voir [la preuve publique](../evidence/public/backend/image-reference-2026-09-20.json). |
| B1 rejeté, B2 repris avec succès | B1 Astra/high : transport réussi en **218,064 s**, 40 parties visuelles ; plan rejeté pour `turn_rate` (`curve` avec `turn_mdeg_s=90000`, alors que zéro est requis hors `homing`). Cet échec est conservé. Après correction du prompt en `2.1`, **B seul a réussi en 204,725 s avec le même PNG G** : plan validé et compilation réussie. Rapport `d13-image-resume-20260920.json`, SHA `74cb91c014f5d07488e7d3ae1a62f26b875e2c85d358b0c2b4d82376097b9c8a` ; paquet SHA `efe24fa464c061b93bdbe7e44195dc513c99b548bce78c0ba325bec9abff7cd5`. Un seul nouvel appel B, soit **quatre appels réels au total : A, G, B1, B2**. Ce diagnostic ne prouve pas un job joueur de production. |
| Référence de développement | [Oiseau d'orage](art-direction/storm-bird-target.png) réellement généré avec l'outil de développement. Ce fichier ne prouve ni le worker du jeu, ni une reconstruction Unity. |
| Version active | [Player `1.3.0`](../game/Build/WindowsImageReferencePlayable/Palimpseste.exe) lancé à **20:11:04 UTC**, PID **39012**, fenêtre réactive et raccourci actualisé. Backend D13 déployé : API **30084**, worker **15760**, `/health/ready` prêt ; diagnostic local sans `production_issues`, `interpreter_production_issues` ni `image_production_issues`. [Preuve publique](../evidence/public/backend/image-reference-deployment-2026-09-20.json). Aucun nouveau parcours joueur D13 complet ni verdict artistique encore enregistré. |

Le [manifeste livré `1.3.0`](../evidence/public/unity/image-reference-delivery.json) atteste **29 fichiers vérifiés, 122 734 785 octets**. Le [ZIP Player](../deliverables/Palimpseste-Windows-x64-IL2CPP.zip) fait **44 320 872 octets**, SHA-256 `0ca7d134f733632f23483726b70fbbeb1de750b046d1478efa94f18d4891ad7b`. Le lancement a précédé un échec d'écriture du manifeste (`Replace($null)`) : correction avec `[NullString]::Value`, vérification du PID/chemin existants puis promotion du manifeste, **sans second lancement**.

Les bornes de construction sont de **64 parties par nœud, 128 par plan et 1024 après expansion**. La référence PNG (512–2048 pixels par dimension, 8 Mio maximum) reste distincte des masques mécaniques. Le nouveau contrat ne permet ni C#, shader, commande, URL ou chemin serveur fourni par le joueur ; les tests et builds demeurent dans le développement. Les anciennes sauvegardes restent utilisables sans régénération.

**Point de reprise : essayer un nouveau dessin dans le jeu ouvert.** Le déploiement local a réussi après correction d'un contrôle PowerShell qui masquait le vrai code PostgreSQL ; la première tentative avait échoué avant tout arrêt de service. Les trois preuves au SHA durci sont promues et le rejet B1 conservé. Les incidents de capture précédents sont documentés, puis la capture 04 a réussi. Suivre [Tester maintenant](TESTER_MAINTENANT.md), observer description → image → sort et recueillir le verdict du joueur. Les durées fournisseur ne mesurent pas un parcours joueur complet ; ce nouveau parcours, la relecture hors ligne D13, la recette humaine et l'accès distant restent à constater. Les sections suivantes sont historiques.

## Historique — D12, spectre magique 1.2.2 livré, verdict artistique ouvert

**Le Player Windows IL2CPP `1.2.2` est construit et livré** dans [WindowsSpectralEnergyPlayable](../game/Build/WindowsSpectralEnergyPlayable/Palimpseste.exe). Le build a terminé avec le **code 0**. Les **29 fichiers du dossier totalisent 122 554 797 octets** ; le [ZIP Player](../deliverables/Palimpseste-Windows-x64-IL2CPP.zip) fait **44 089 295 octets**, SHA-256 `6836f2cac5941a58d28b5a07c1b7f5e0b88cc5c51430320a6c9d9d199c1c542e`. Voir le [manifeste de livraison](../evidence/public/unity/spectral-energy-delivery.json). Le nouveau Player a été lancé sous le PID `13972` et le raccourci Bureau **Palimpseste Spell Lab** est actualisé.

Le créateur avait refusé le spectre `1.2.1`, puis **explicitement rejeté l'itération 02** pour son corps trop solide. Quatre itérations visuelles ont suivi la demande de magie translucide, d'animation organique et d'impact. La version finale remplace la capuche et le visage opaques par de la brume volumétrique et des courants lumineux. Six nappes suivent l'historique réel du vol ; le sillage se dissipe durant **0,62 s** après retrait du porteur mécanique, sans nouvelle touche. La caméra du laboratoire et le paquet joueur sont conservés. Aucun appel modèle n'a été nécessaire.

Les [preuves D12](../evidence/public/unity/spectral-fidelity-2026-09-20.md) consignent les captures : **01 : 1/1, 02 : 1/1, 03 : 5/5, 04 finale : 1/1**. La capture finale rejoue le vrai spectre avec une touche, 12 000 unités internes de dégâts et une impulsion. Deux vidéos de **73 images, 30 images/s, 2,433333 s**, sans son, montrent [la caméra du labo](../evidence/public/unity/spectral-fidelity/iteration-04/spectre-lab.mp4) et [une caméra latérale de comparaison](../evidence/public/unity/spectral-fidelity/iteration-04/spectre-side-review.mp4). Cette seconde caméra est réservée aux preuves ; la cadence fixe de capture ne constitue pas un benchmark de FPS.

**Point de reprise : verdict du créateur sur le spectre conservé dans `1.2.2`.** La correspondance exacte « 1 pour 1 » à la référence n'est pas démontrée, et aucune acceptation humaine du nouveau rendu ou du son n'est enregistrée. Rejouer ce sort ne consomme aucun appel modèle. Les autres styles et recettes ne sont pas déclarés artistiquement validés par cette reprise ; aucun nouveau parcours dessin → modèles → Player, autre poste ou accès de joueurs distants n'est attesté ici. Les sections suivantes décrivent les livraisons historiques.

## Historique — D11, Player stylisé 1.2.1 livré puis spectre refusé

Le retour du créateur est que 1.2.0 convient à une démonstration mais reste trop simple visuellement. La version 1.2.1 ajoute des compositions de zones, des spirales ascendantes et des couronnes, quatre familles de particules, des rubans translucides et un éclairage HDR maîtrisé. Le rythme visuel d'un soin exploite ses effets `heal`/`regen`. Le backend, les prompts et les données de sort ne changent pas ; **aucun appel fournisseur** n'a été effectué pour cette finition. Voir [D11](DECISIONS.md).

Les [preuves Unity D11](../evidence/public/unity/stylized-vfx-2026-09-20.md) comprennent **neuf images de fixtures** (soin vert, impulsion violette, projectile de feu) et **six images du vrai spectre conservé**. Celui-ci conserve sa touche, ses 12 000 unités internes de dégâts et son impulsion. La deuxième exécution a donné **5 réussites sur 6** ; le seul échec de galerie a été corrigé, puis cette galerie seule a passé **1/1**, y compris après la dernière retouche des zones. Les erreurs initiales de courbes de particules et de borne de sinus sont consignées ; aucun passage unique 6/6 n'est revendiqué. Les captures sont produites sur RTX 3070 avec le postprocessing réel, et ne constituent pas un benchmark de FPS.

**Le Player Windows IL2CPP `1.2.1` est livré** dans [WindowsStylizedVfxPlayable](../game/Build/WindowsStylizedVfxPlayable/Palimpseste.exe). Le build a terminé avec le code 0, sans avertissement ni erreur shader trouvés dans son journal. Ses **29 fichiers vérifiés totalisent 122 476 445 octets** ; le ZIP fait **44 217 522 octets**, SHA-256 `8c92040e5abdbc8d690216010cafd9522f824dca7c9cd7db6aa42c4c82a32c0c`. Voir le [manifeste de livraison](../evidence/public/unity/stylized-vfx-delivery.json). Le Player a été lancé sous le PID `30968`, fenêtre réactive ; le raccourci Bureau est actualisé. La santé API répond 200 et le backend reste celui de D10.

**Point de reprise D11 : verdict du créateur dans le laboratoire.** Reprendre un sort conservé pour apprécier la finition sans nouvelle génération. Le verdict artistique et le son restent ouverts ; aucun essai exhaustif de toutes les combinaisons de styles et recettes, benchmark de charge, nouveau parcours joueur complet dans `1.2.1`, autre poste ou accès HTTPS distant n'est attesté. Les preuves D10 suivantes décrivent la version précédente.

## Historique — D10, intégration Sol → Astra et VFX composés, 20 septembre 2026

La dernière demande choisit **Sol/high pour interpréter le dessin, puis Astra/high pour composer le sort**, avec les prompts A `2.2` et B `1.9`. Elle demande aussi des VFX plus lisibles et spectaculaires et une attente plus courte. Le code ajoute le profil décoratif borné `appearance.vfx`, un client `1.2.0`, des couches animées autour des formes 3D et un affichage du temps écoulé réel. Les anciens sorts sans profil restent compatibles ; leur forme et leur palette déterminent une composition visuelle par défaut dans le nouveau renderer, sans régénérer leur description. Voir [D10](DECISIONS.md) et [la direction VFX](art-direction/VFX_COMPOSITION.md).

Les projections envoyées au modèle sont réduites et les schémas minifiés, en conservant les contraintes et le catalogue complet du compilateur. Le [diagnostic actif réel](../evidence/public/backend/sol-astra-active-2026-09-20.json) a réussi sous `PalRuntimeSvc` : A Sol/high **31,177 s**, B Astra/high **29,226 s**, soit **60,403 s d'appels**, sans réparation, plan validé et compilation réussie. Le diagnostic complet a duré environ **63 s**, sur les images du dessin qui avait demandé **285 s** avec Astra/max → Luna/max. Ce seul essai ne garantit pas le délai des prochains dessins ni celui du parcours complet dans le Player ; voir [la comparaison détaillée](../evidence/public/backend/sol-astra-latency-2026-09-20.md). Une génération joueur ne lance aucun test ni build Unity.

**Le backend D10 est déployé localement** ; santé HTTP 200, API PID `8932` et worker PID `19656` sous `PalRuntimeSvc`, porte technique du couple approuvée. Les 17 fichiers de spécification correspondent ; voir [le relevé de déploiement](../evidence/public/backend/sol-astra-deployment-2026-09-20.json). Empreintes des exécutables : API `6168F278C0070F9ED286050DCD82F8BBAB4D8F273FB7A351D55412BCA8553923` ; worker `FC78432166EAED3E646885F68B36832C2F1FC691AE4F271017D2056FD76530DA` ; doctor `F0197C22267F5C91760BC079E7A43D986A942938A533E0D8868394390BBE1F0F`. Les modèles et efforts sont réellement rapportés par les deux appels du diagnostic. Cette preuve technique ne vaut pas acceptation artistique.

Le rendu a passé **5/5 tests PlayMode ciblés en 7,94 s**. Une [relecture graphique du vrai sort conservé](../evidence/public/unity/composed-vfx-2026-09-20.md), « Envol du spectre aux longs voiles », fournit six images successives, un impact, **12 000 unités internes de dégâts** et une impulsion enregistrée. Elle n'utilise aucun nouvel appel modèle et ne modifie pas le paquet joueur. Le déplacement de cible mesuré est faible (environ 1,54 mm) : cette preuve ne démontre pas une projection spectaculaire. L'image [spectre de référence](art-direction/spectral-veils-target.png) reste un concept généré, **pas une capture du jeu**.

**Le Player Windows `1.2.0` est construit et livré** dans [WindowsComposedVfxPlayable](../game/Build/WindowsComposedVfxPlayable/Palimpseste.exe). Après correction ciblée des bornes `pow`, le build IL2CPP/URP final a terminé avec le code 0, sans avertissement shader trouvé dans le [journal final](../evidence/public/unity/composed-spectre/build-windows-final.log). Le dossier jouable contient **29 fichiers, 122 417 345 octets** ; le [ZIP](../deliverables/Palimpseste-Windows-x64-IL2CPP.zip) fait **44 189 567 octets**, SHA-256 `A8B137D587F6A4F2D3677C06C38D4BE7BAAA280183AE52CBEB3622E201A42B68`. Les 29 fichiers sont contrôlés dans le [manifeste de livraison](../evidence/public/unity/composed-vfx-delivery.json), hors les deux dossiers `DoNotShip`. `GameAssembly.dll` a pour SHA-256 `463D73EEA6086E963DA3B73A5C7A04CE9281A303D3B63BEB1444B426B5F0F256`. Le Player a été lancé en fenêtre normale sous le PID `32064` ; le raccourci Bureau « Palimpseste Spell Lab » pointe sur cette livraison.

**Point de reprise D10 : essai et verdict du créateur.** Rouvrir le spectre conservé dans ce Player, puis essayer un nouveau dessin pour apprécier fidélité, rendu, son et délai réel. Aucun verdict artistique humain, parcours complet d'un nouveau dessin dans ce Player final, recette des 30 dessins ou essai sur un autre poste n'est enregistré. Le déploiement demeure local ; l'accès HTTPS de joueurs distants reste à réaliser. Les empreintes et PID des sections suivantes appartiennent aux livraisons historiques.

## Historique — objet 3D interprété D09, Player Windows livré, 20 septembre 2026

La [décision D09](DECISIONS.md) demande que le sort montre **l'objet imaginé par Astra en volume** — rocher, arme, créature ou énergie — avec un rendu propre et impressionnant. Le nouveau dessin reste l'entrée multimodale, mais sa silhouette n'est plus automatiquement recopiée pour former l'objet 3D. Les contrats ajoutent **21 formes contrôlées** `visual_form` dans A `2.1`, copiées en `appearance.form` par B `1.8`, avec une géométrie sémantique distincte par sujet. Les paquets anciens restent lisibles. Les recettes d'effet et les limites d'exécution restent contrôlées ; le parchemin n'apporte pas de code.

Le code partagé compile sans avertissement ni erreur ; le smoke Core valide les **141 recettes / 705 couples recette-porteur** et les cas de géométrie sémantique, et la QA des contrats passe **61/61**. Le renderer Unity a passé **5/5 tests PlayMode ciblés** (quatre fonctionnels et une capture graphique ; XML SHA-256 `4AC0BCF958F306551E50292FB57F840E625FD432BC2804AC6DE151321CE63F5A`) ; le cache hors ligne a passé **1/1 EditMode**. Une [capture de démonstration](../evidence/public/unity/semantic-forms-preview-20260920.png) a été inspectée par les agents ; elle provient d'une fixture Unity, pas d'un nouveau sort joueur, et ne vaut pas verdict humain.

Le backend D09 A `2.1`/B `1.8` est **déployé** sous `PalRuntimeSvc` ; API PID `19592`, SHA-256 `E98D511270AC2E6722F1946A368730968F640EFE569B476073DC33C521F9E194` ; worker PID `5532`, SHA-256 `19DBB0856F0504B4A51230E35086D4D44C9026135211B979A375890A9DCD7EDA` ; doctor SHA-256 `C4A516C5257DCC48BAFEA6266C38C6E4D8D2B25838E5C7B65F7E233DC8733EF9`, local terminé avec code 0. Un [diagnostic A/B réel](../evidence/public/backend/semantic-visual-generation-2026-09-20.md) a donné **« Ruée du golem au poing-bélier »**, `golem`/`stone`/`r_masse_repulsive`. Un premier B donnait un rayon `1` trop petit ; après la borne minimale par forme (45 pour `golem`), B `1.8` a produit un plan `radius=55`, `scale=180`, `speed=1000`, `lifetime=180`, `range=1800`, `signature_geometry_id=null`, validé et compilé **sur la même A figée**. Ce diagnostic n'est pas un nouveau job joueur.

Unity 6000.3.24f1 a construit le [Player Windows 3D final](../game/Build/WindowsSemanticVfxPlayable/Palimpseste.exe) en IL2CPP/URP avec code 0, `PALIMPSESTE_BUILD_OK` et le matériau d'émission conservé ; [preuve du build et des tests](../evidence/public/unity/semantic-visuals-2026-09-20.md). Le dossier jouable contient **29 fichiers, 122 350 769 octets**, identiques en SHA-256 au build brut hors `DoNotShip` ; `GameAssembly.dll` SHA-256 `59124E2AE6463AA29499EE1043AAD816E0D8ACAFDBB2D85F13634F7A1E73B6C1`. Le [ZIP Player](../deliverables/Palimpseste-Windows-x64-IL2CPP.zip) fait **44 157 438 octets**, SHA-256 `4904A3F42D52D14683D7577C3A05829E49317B1C12572093DACE6339326464E6`, avec 29 entrées comparées sans écart. Le Player a été lancé sur ce PC à 16:53:38 (Paris), PID `26864`, fenêtre `Palimpseste Spell Lab`, `Responding=True` ; aucun clic ou nouveau dessin n'a été injecté. **Aucun parcours joueur complet sous D09 ni verdict humain sur le rendu 3D, le son ou la fidélité ne sont encore attestés.** Le premier build sémantique sans matériau d'émission a été conservé en historique, hors du ZIP livré.

Le [ZIP backend opérateur final](../deliverables/Palimpseste-Backend-Windows-x64.zip) fait **111 439 621 octets**, SHA-256 `44D25C9986B4FDA4853750DBD6922698A8D1187DE956BDDF6A393348EE15E764` ; ses **107 fichiers** correspondent au stage par SHA-256, les pins API/worker/script enfant sont cohérentes et aucun secret, `.env`, `.pdb` ou `auth.json` n'y figure. Son installation sur une autre machine n'a pas été testée. Le service en cours reste local sur `127.0.0.1`.

## Historique — répétition perçue de l'interprétation, 20 septembre 2026

Deux dessins récents différents ont produit des titres Astra proches, ce qui confirme un problème de variété perceptible à examiner. Le job `997d0b50-602a-4668-981e-a39108334bf4` a donné **« Nœud de cuivre »**, porteur `projectile`, recette `r_crochet_entravant` ; le job `e6d656f0-4a13-4ba5-9f44-3b4e7d2f6069` a donné **« Nœud de cuivre mordant »**, porteur `trap`, recette `r_estoc_saignant_lien`. Les captures ont des SHA-256 distincts (`CE0389…` et `18B46D…`), comme leurs descriptions (`F1E12E…` et `16EEC8…`). Les deux jobs sont `ready` : il ne s'agit pas du même sort rejoué à l'identique, même si leurs titres se ressemblent. Voir [le relevé comparatif](../evidence/public/backend/interpretation-repetition-2026-09-20.md).

Le prompt Astra A `1.9` contenait un exemple « rouge brun → rayon de lave » et associait aussi le rouge brique au cuivre ; ces exemples pouvaient orienter la lecture de plusieurs dessins faits avec la même encre. Cette influence est une **hypothèse**, pas une causalité démontrée par deux sorties. A `2.0` a retiré cet exemple et demandé de lier le titre et les mécaniques à deux particularités de l'image. Un **troisième job joueur réel avec A `2.0`**, `7851777c-ddb1-49eb-84f2-641463b15364`, est `ready` : titre **« Le Nœud filant »**, `projectile`, `r_crochet_entravant`, palette `arcane`, description SHA-256 `FEE3AB9390F28BAA810432855F383DEE39A2D701669B533376EBD144D00F0106`. Ce résultat montre que le thème « nœud » persiste malgré A `2.0` ; il ne prouve pas une cause unique ni une répétition de sortie identique. A `2.1`, désormais déployé, précise en plus que des croisements de croquis ne signifient pas nécessairement des fils ou des cordes, et demande de lire d'abord les masses et la silhouette, puis l'objet. Le diagnostic réel ci-dessus prouve la génération technique d'une forme `golem`, **pas** une lecture variée acceptée par le créateur ni un lancement du Player final. Le verdict de fidélité lui appartient.

## Historique — suppression du quota quotidien de l'application, 20 septembre 2026

Le refus affiché sur le dernier dessin (« Votre quota quotidien de générations est atteint ») provenait du plafond **propre à Palimpseste** : le service local avait admis cinq jobs joueur sur cinq permis en 24 heures, pour six jobs globaux. Ce refus n'indiquait pas que le compte Codex était épuisé. La capture du joueur est conservée localement sur le même profil Windows ; l'écran propose **Transmettre**.

Le correctif retire les contrôles de nombre de jobs en 24 heures pour la capture et le diagnostic de plan par créateur. Les variables de configuration de ces plafonds ne sont plus utilisées ; les anciens jobs et la migration PostgreSQL restent conservés. La solution a été compilée avec **0 avertissement et 0 erreur**, et la QA des contrats a passé **61/61**. L'API corrigée a été publiée sous `PalRuntimeSvc` (PID historique `35728`, SHA-256 `1E530DAE4DB1680BBDB7E8388DF825D215C5BCF5B17DE50A7B5E4ACD4FA64B7C`) ; son script enfant `ApiService.Child.ps1` avait pour SHA-256 `3655615CF544B6E757F33FC61B773C83943935E06B0C70559B7B20E2EA2D1762`. `/health/ready` répondait 200. Lors de cette correction, aucune nouvelle capture HTTP n'avait encore été retentée ; les trois jobs `ready` observés ensuite sont décrits en tête de document. Voir [la portée et l'historique du quota](GENERATION_QUOTA.md).

Les limites du compte Codex relié au worker restent indépendantes et peuvent interrompre une génération. Le code du jeu ne commande aucun achat ni recharge ; l'état réel de la recharge automatique se vérifie dans le compte ChatGPT, hors de l'application. Les sections suivantes gardent l'état des 141 recettes avant cette correction.

## Historique — déploiement initial des 141 recettes, 20 septembre 2026

La [bibliothèque des recettes](../contracts/effect-recipes.json) contient désormais **141 sorts mécaniquement distincts** : 119 hostiles, 21 de soutien et un réservé à la caisse destructible du laboratoire. Chaque recette fixe sa cible et une combinaison de une à quatre des **24 primitives** exécutées par Unity, avec montants et durées bornés. Les 141 signatures diffèrent par cible et ensemble d'effets ; changer seulement un nom, une couleur ou une valeur ne crée pas une nouvelle recette. Les cinq porteurs déclarés pour chaque recette donnent **705 couples recette/porteur**. Le compilateur a validé et compilé ces 705 couples dans le smoke Core ; la QA des contrats a passé **61/61**. La [projection compacte](../contracts/effect-recipes-prompt.json) donne à Astra les IDs et effets disponibles ; Luna doit produire les primitives exactes de la recette choisie, et le compilateur rejette les écarts. Voir [la bibliothèque et ses sources d'inspiration](EFFECT_LIBRARY.md).

La compilation des 705 couples est une validation **hors modèle** des données et du compilateur. Depuis ce déploiement initial, **trois jobs joueur réels ont parcouru Astra → Luna avec ce catalogue** et ont atteint `ready` : `997d0b50-602a-4668-981e-a39108334bf4`, `e6d656f0-4a13-4ba5-9f44-3b4e7d2f6069` et `7851777c-ddb1-49eb-84f2-641463b15364`. Aucun lancement de ces trois sorts dans le laboratoire n'est attesté ici. La qualité de l'interprétation, le VFX, le son et l'intérêt de chaque combinaison restent soumis aux essais des créateurs. Les 46 concepts `design_only` du catalogue cible restent hors production.

Le backend des 141 recettes est **déployé sur ce PC** sous `PalRuntimeSvc` avec les prompts Astra `1.9` et Luna `1.6` : API PID `22024`, SHA-256 `8B280D15ADEA95D4322C8957913ED3A46659221E208A4938B391E9CDBFE1E825` ; worker PID `27988`, SHA-256 `7FEEE56F6ECD63741B9AC129D16ACF721ADCF2EF6B869ECFFD5068429643FDC7` ; doctor SHA-256 `CBBC49DD26B6BDC7635482C9B7D776A031C5BD0A39E3FBF22F9D3A034800C04D`. Les 16 contrats copiés, les trois prompts et les quatre exécutables/scripts vérifiés correspondent aux sources par SHA-256. `/health/ready` répond 200. Le doctor local s'est terminé avec code 0, `production_issues=[]` et `interpreter_production_issues=[]` ; rapport privé SHA-256 `77254BC9C644F82F8E3D2E54A6215AE5A06BF9C74A426134C4CD4D241F94C6E5`. Une sauvegarde avant déploiement est dans `E:\PalimpsesteBackups\pre-recipe141-20260920`. L'API reste accessible seulement sur `127.0.0.1` ; HTTPS public et plusieurs joueurs distants ne sont pas livrés.

Les sections ci-dessous décrivent les builds et services antérieurs quand elles citent d'autres empreintes ou versions de prompts.

## État courant — reprise du sort et bibliothèque d'effets, 20 septembre 2026

Le dessin joueur « Enclume filante » est **ready** sous le job `e3537609-1196-47d6-8750-fe79124f1a0a`, avec le sort compilé `43e21914-8f59-4fcd-803c-27385e530fd1`. Astra A avait réussi. Le premier B avait été refusé avant génération par HTTP 400 `invalid_json_schema` : `appearance.palette` était présent dans `properties` mais absent de `required`. Le schéma strict B et le prompt ont été corrigés ; une transaction ciblée a repris **le même dessin**, sa description et ses géométries sans effacer les tentatives. Le B suivant a réussi sous `gpt-5.6-luna/max` demandé. L'API expose désormais une reprise bornée par le propriétaire pour ce type d'échec B ; le client propose « Réessayer ce dessin » seulement lorsque le serveur la permet. Voir [la preuve de reprise](../evidence/public/backend/enclume-schema-recovery-2026-09-20.md). Le sort « Enclume filante » n'a pas été lancé manuellement dans le labo pendant cette correction ; l'écran du joueur peut charger sa fiche avec « Actualiser ».

Le [catalogue actif](../contracts/capability-catalog.json), les schémas, les prompts Astra `1.8` et Luna `1.5`, le compilateur et le runtime Unity déclarent **24 effets contrôlés** : six initiaux et dix-huit nouveaux, dont hémorragie, poison, vol de vie, régénération, bouclier, contrôles et destruction d'une caisse de labo balisée. La [bibliothèque cible](../docs/EFFECT_LIBRARY.md) contient 70 concepts au total ; 46 restent `design_only` et ne sont pas proposés à Astra comme jouables. Core smoke et QA contrats `61/61` ont réussi ; les tests PlayMode ciblés des effets ont passé **6/6** (XML SHA-256 `6AD9939F3D953C1B60C6B43A5EE4E8D4E7D6D235F579C58189C6DBFB69889A04`). Cela vérifie des familles de mécanismes, mais ne remplace ni l'essai humain de chaque apparence ni une recette exhaustive des combinaisons.

Unity 6000.3.24f1 a produit le nouveau build Windows IL2CPP/URP `game/Build/WindowsEffectLibraryReady/` avec `PALIMPSESTE_BUILD_OK` et retour 0 déclaré dans le journal SHA-256 `70269EC39F8976455846405ECC8AF58EE3FD2F84595D9293705A7EC39F985F9B`. Le [dossier jouable](../game/Build/WindowsEffectLibraryPlayable/Palimpseste.exe) contient 29 fichiers, 122 136 545 octets, tous identiques en SHA-256 aux fichiers correspondants du build brut ; `GameAssembly.dll` SHA-256 `A003ED515F512965E5D4A88E0151FB976157DD0F24FE7E155302D8E3A5658B8F`. Le ZIP Player de cette epoque contenait les 29 fichiers, 44 054 317 octets, SHA-256 `8F8ADC123388801065234D0E1179D137C03B0B574D80F5D2B8A2690BBF768587`. Le nouveau Player a été lancé sans interaction manuelle ; le VFX des nouveaux effets n'a pas reçu de verdict visuel humain.

L'API locale et le worker sous `PalRuntimeSvc` sont déployés avec ces 24 effets : API SHA-256 `5114D6E637553A4B2C364D2A1C89C147F242B37C561AB65946DD831FF8F1E3AA`, worker SHA-256 `DE976A43401C59938573B83D9ECEEB67E35AE222A3EA640AB214B40C0B0AC4FB`, doctor SHA-256 `11B5BFB7C718200F903C2BB11A7D76990CDCE1BC979821399643D693E1993D39`. `/health/ready` répond 200 ; le doctor local a terminé avec code 0, `production_issues=[]` et `interpreter_production_issues=[]` (rapport SHA-256 `4D1F52B4F192556A0BF6507CC5FE45C37B588580E34A505EB695AF134468F864`). **Aucun nouvel appel modèle réel avec les 24 effets ni verdict humain sur ce build n'est attesté.** Le service reste sur `127.0.0.1` ; autre PC, HTTPS public et recette de 30 dessins restent ouverts. Les sections datées ci-dessous conservent l'historique des builds antérieurs.

## État courant du dessin libre — 20 septembre 2026

Le build courant [WindowsAppearanceVfxPlayable](../game/Build/WindowsAppearanceVfxPlayable/Palimpseste.exe) accepte plusieurs traits sur tout le parchemin ; seul « Dessin terminé » ferme la capture. Le rendu du sort dispose maintenant d'une palette contrôlée par sujet, issue des nouveaux contrats A `1.7` et B `1.3`, avec compatibilité pour les anciens sorts sans palette. Unity 6000.3.24f1 a terminé le build Windows IL2CPP/URP avec le code 0 et `PALIMPSESTE_BUILD_OK` : dossier nettoyé de 29 fichiers, 122 113 753 octets ; EXE SHA-256 `049F79454586F2AC5445F26B55191CF6611BE62F10C4A5E12F92F806050149C2`, `GameAssembly.dll` SHA-256 `BBA75A8B708DD404D877CC7E4CA5A3448049F391EC9C5AACB4446E03EF54FF55`. Journal de build SHA-256 `57AC953C7E0890B68BEFB9B0B7875E1198EFA16139D6CADF523903AEC571877B`. Le test PlayMode ciblé du VFX et de la palette a passé 1/1 (rapport SHA-256 `63F8A0A252BD9DDF7177D20E21239BE5B96BAF11E0D2A55E71ABAF4A3CF996AD`). Les tests antérieurs du dessin libre et des erreurs API ont passé 17/17 EditMode et 2/2 PlayMode ciblés ; le smoke HTTP `free_canvas_v2` a passé 16 contrôles. Le nouveau VFX n'a pas encore été inspecté visuellement dans ce Player.

Le compte `PalRuntimeSvc` a exécuté **un appel réel** `codex exec` multimodal `gpt-6-astra`/`max` avec deux images distinctes : modèle et effort rapportés conformes, code de sortie 0, description structurée valide. La preuve technique SHA-256 `A43D24B23AD0ABE1378B708C4DC47D6FB3C487451CB39CC10F56C47AD8519D77` est approuvée dans `E:\PalimpsesteRuntime\approved-evidence`. La migration 007 est appliquée après sauvegarde `E:\PalimpsesteBackups\pre-free-canvas-stopped-20260920`. Le job synthétique `769eea5b…` a échoué en `needs_operator/plan_invalid` : sa description Astra mêlait un faisceau à un effet stationnaire ; le plan Luna initial et deux réparations contrôlées ont été rejetés. Le validateur pré-B a ensuite été corrigé. L'API (SHA-256 `6F7ABB5A4B86DD718FF77532DCF25C57F024FBE4F2F9EBA24F29642CB35F238B`) et le worker (SHA-256 `737E1CC61E5D6B1E5B235D627616C7CAD0F535464385A545DD8D2BBC246D8D48`) avec prompts A `1.7`/B `1.3` sont déployés sur le service local ; `/health/ready` répond 200 et le doctor local rapporte zéro problème de production A/B. Aucun appel modèle réel avec les nouveaux champs de palette n'est encore attesté. La sonde Astra reste une calibration technique, sans validation artistique.

Le Player du dessin libre a été piloté visuellement : un clic sur « Dessin terminé » sans trait a laissé la page ouverte ; deux traits séparés sont restés visibles après deux relâchements ; le clic final a conservé le dessin. Après un premier refus HTTP 429, la capture a été transmise sous le job joueur `cd4fec7e-aae8-45ba-b5f3-16a807a9771e`. Astra et Luna ont réussi, le job a atteint `ready`, et le Player précédent a affiché « Estoc à crochet » puis la fiche du sort compilé. Un lancement visible dans le laboratoire a été capturé dans ce build précédent ; les clics manuels supplémentaires sur les cibles ont été arrêtés à la demande du créateur. Le Player `WindowsAppearanceVfxPlayable` avec le nouveau rendu n'a pas encore été évalué visuellement. Aucun verdict humain n'est enregistré pour le nouveau VFX, le son ou la fidélité globale ; la recette de 30 dessins, le second créateur et une autre machine restent ouverts. Voir [les captures et le rapport du dessin libre](../evidence/public/unity/free-canvas-appearance-2026-09-20.md). Les anciens résultats et PID mentionnés plus bas sont historiques.

**Priorité de validation actuelle :** examiner le VFX de « Estoc à crochet » dans [le nouveau Player](../game/Build/WindowsAppearanceVfxPlayable/Palimpseste.exe), en le comparant à la description Astra. Ce Player a été relancé sans interaction manuelle ; le test PlayMode vérifie techniquement le rendu et la palette, mais la capture du lancement précédent ne valide pas ce nouveau rendu. Le son, la physique et la relecture hors ligne restent à mesurer sur ce build. Le ZIP Player de cette epoque a été régénéré depuis le dossier nettoyé (44 045 012 octets, SHA-256 `4AF6EF5D7449CBE89094755A3B6A045D5EF75DA21706C40484403FB17B419A3F`). Le ZIP backend de cette epoque a aussi été régénéré avec la chaîne Astra → Luna ; il est réservé à l'opérateur et son installation sur un autre PC n'est pas testée.

Mis à jour le 20 septembre 2026 après la première boucle joueur réelle et l'incident d'un nouveau dessin conservé localement. Les observations de builds, ZIP et sondes antérieurs restent historiques lorsqu'elles sont datées comme telles ; l'état courant est celui ci-dessous.

Preuve détaillée du cas actuel : [parcours joueur, build et relecture sans API](../evidence/public/unity/owner-player-end-to-end-2026-09-20.md).

**Boucle observée.** Un dessin rouge-brun courbe capturé dans le Player a produit un job `production` `ready`. La base indique deux tentatives fournisseur réussies, une description A, un plan B et un `compiled_spell`, sans échec. Le Player a affiché « Faisceau courbe de lave », téléchargé et vérifié la fiche, ouvert le laboratoire et compté un lancement (`Lancers : 1`, `Dégâts : 0`). La génération a utilisé le worker `PalRuntimeSvc` et `codex exec` avec `gpt-5.6-luna` et `max` rapportés. Le trait droit de calibration A `1.3` est un autre dessin : son B `1.1` repris sur la géométrie réelle et ses tests isolés restent des preuves de mise au point, distinctes de ce paquet joueur.

**Build et relecture locale.** Unity 6000.3.24f1 a terminé avec le code 0 le build Windows IL2CPP/URP `game/Build/WindowsPlayerBeamVisibleReady/` : 29 fichiers, 122 093 149 octets, EXE SHA-256 `049F79454586F2AC5445F26B55191CF6611BE62F10C4A5E12F92F806050149C2`, `GameAssembly.dll` SHA-256 `9FB651D09680CAF09B0A8668BEF2A914EFDE6B61F930EDBAE1CE90AF3B5C18C9`. Le paquet joueur a passé 2/2 tests PlayMode du correctif de visibilité à 0,6 s. Une capture du nouveau Player montre l'effet visible puis disparu. Après arrêt de l'API PID `25848`, le Player a été relancé sous PID `35804` : bibliothèque, fiche, laboratoire et lancement du même sort ont fonctionné sans API. L'API a été redémarrée sous PID `16420` et `/health/ready` répond 200 ; le worker PID `8180` est resté actif. Cet essai couvre l'indisponibilité de l'API, sans évaluation humaine du son.

**Nouvelle capture en attente et build correctif.** Le Player PID `35804`, démarré pendant l'indisponibilité de l'API, a conservé le nouveau tracé fermé en `capture_pending` avec `needs_begin=true`, `needs_capture=true` et 151 entrées de journal. Lorsque l'API répondait de nouveau 200 et le worker était actif, aucun nouveau job n'existait pour cette capture ; aucune lecture Luna de ce second dessin n'a commencé. Le code ajoute « Reconnecter » pour rétablir la session puis rouvrir le même parchemin. Unity a produit `game/Build/WindowsPlayerReconnectPlayable/` (29 fichiers, 122 095 741 octets ; EXE SHA-256 `049F79454586F2AC5445F26B55191CF6611BE62F10C4A5E12F92F806050149C2`, GameAssembly SHA-256 `E379AD0EC9CB79533E319BDEB97826E480BE55D614697A66F4183AB81669D75F`). Le journal Unity contient `PALIMPSESTE_BUILD_OK` et « Application will terminate with return code 0 », sans erreur `CS`; le code de retour PowerShell parent n'a pas été capturé. Les 29 hashes du dossier jouable égalent ceux du build brut, hors deux dossiers `DoNotShip`. **Le nouveau Player et la reprise de cette capture n'ont pas encore été exécutés ou validés.** Voir la [preuve de l'incident et du correctif](../evidence/public/unity/player-reconnect-pending-capture-2026-09-20.md).

**Sécurité et limites.** Le worker installé a pour SHA-256 `9243F9600973B1AAF5F97F07AD0983FF60A7EFA2EB0A7F7D42D142C05015847D` et son script enfant `03CE0108D67A4AC27B332A41F36F1F12B2B362FA9A2AEBCF25EB21E8BDB4C104`. Le Doctor installé SHA-256 `AAAAB2686CD9A33ADB6130B205D10596A63AF8207CBAE580ACD723C862196811` a passé son diagnostic local après correctif BOM, sans appel modèle ni `production_issues`. Le manifeste composite v2 SHA-256 `56F349016A5B63D352DD37225AF9253889D496C2399F58F97F70D0DAAC353E3F` lie les appels réels A/B et leur validation/compilation sur les octets d'origine ; `PALIMPSESTE_EFFORT_VERIFIED=true`. Les originaux A/B/encre restent nécessaires et protégés. Les migrations 003 à 006 sont appliquées ; la génération et les secrets restent dans le compte de service isolé, hors du Player et du dépôt.

**Portes humaines.** L'auteur avait rejeté la première lecture « faisceau de feu visuel » du trait de calibration et précisé « laser unidirectionnel de lave ». Aucun verdict positif n'est encore enregistré sur A `1.3`, sur le dessin courbe produit, sur la qualité visuelle, ni sur le son. Les 30 dessins inédits, le second créateur et la machine Windows propre de M7 ne sont pas réalisés. Le laboratoire reste local au propriétaire ; l'usage d'un abonnement individuel commun pour des joueurs externes et les réglages de recharge du compte demandent une vérification distincte. Aucune URL HTTPS publique n'est déployée. Les refus `blocked by policy` avant exécution ont concerné la première commande de lancement de l'API, la création du ZIP et une commande de confort pour fermer puis relancer le Player après restauration de l'API. Le lanceur sûr a résolu le démarrage API ; le dossier Windows exécutable constitue la livraison actuelle sans nouveau ZIP. Le Player PID `35804` est resté ouvert dans sa session issue de l'essai sans API ; aucune relance avec API restaurée n'est revendiquée.

## Échelle employée

`prévu` = aucune source livrée ; `implémenté` = source écrite ; `compilé` = outil de compilation exécuté avec succès ; `testé auto` = essai automatisé exécuté ; `Luna réel` = appels A/B observés avec les images et le compte de service ; `Unity exécuté` = scène et build lancés ; `accepté humainement` = verdict enregistré par un créateur. Un statut supérieur n'est jamais inféré d'un statut inférieur.

## Environnement observé

- SDK .NET 10.0.401, PostgreSQL 17 et Unity 6000.3.24f1 avec module Windows IL2CPP observés. Unity Personal était active. Les builds et tests historiques sont consignés dans `docs/UNITY_TEST_PROOF.md` et `evidence/`.
- Codex CLI patché `0.154.0-alpha.6.2`, compte ChatGPT dédié à `PalRuntimeSvc`, `CODEX_HOME` isolé. Le diagnostic local observe les fonctions d'exécution du modèle désactivées et les refus d'accès à la source ; `unified_exec` demeure un mécanisme interne du CLI. La preuve d'effort composite v2 est approuvée techniquement ; les verdicts de fidélité ne le sont pas.
- L'API locale `127.0.0.1:18080` répondait 200 lors du diagnostic du second dessin, et le worker était actif ; le Player `WindowsPlayerBeamVisibleReady` PID `35804` avait été lancé sans API. Le second dessin est resté local sans nouveau job. `WindowsPlayerReconnectPlayable` est construit mais son lancement reste à vérifier. Aucun endpoint public n'est ouvert.
- Le coffre Windows du Player protège le jeton client sur ce compte ; le coffre de base et les identifiants du service restent hors du dépôt. L'autre compte Windows, l'autre poste et l'exposition multi-joueurs ne sont pas testés.
- Les essais de sécurité, quota et reprise de base sont documentés ; une panne fournisseur ou PostgreSQL réelle pendant un nouveau job reste à éprouver. Les anciens ZIP et mesures d'endurance portent sur des builds précédents.

## Matrice exigences → sources → preuve → état

| Ticket | Sources principales | Preuve actuellement observée | État actuel |
|---|---|---|---|
| B01 | `game/`, `shared/`, `backend/` | Dessin réel → A/B Luna → plan/paquet → texte/fiche/labo et lancement dans le Player ; job `ready`. Build BeamVisibleReady et relecture sans API observés. | boucle privée exécutée, acceptation humaine ouverte |
| B02 | `shared/Palimpseste.Contracts`, `shared/Palimpseste.Core` | compilation .NET et smoke fixture | compilé, testé auto partiel |
| B03 | `contracts/human-review.schema.json`, `backend/Palimpseste.Api`, `backend/migrations/002_review_access.sql`, `tests/review_http_smoke.py`, `tests/review_access_http_smoke.py` | route de revue liée au principal authentifié ; smoke HTTP/DB à deux comptes creator : délégation sort/cas exacte, soumission du second compte, lecture du sort toujours propriétaire, révocation et nettoyage ; rejet humain de la **première** lecture A exprimé dans la conversation, sans revue signée dans l'API ni corpus de recette | testé auto partiel, verdicts A actuels ouverts |
| B04 | `game/Assets/Palimpseste/Drawing` | canevas, trois brosses et transformation pointeur codés ; dessin réel à la souris capturé dans le lecteur ; équivalence entre cadences non mesurée | exécuté Unity partiel |
| B05 | `game/Assets/Palimpseste/Drawing`, `shared/Palimpseste.Core/PngCodec.cs` | PNG roundtrip fixture ; journal haché durable généré par le lecteur et accepté dans une capture HTTP réelle ; reprise simulée après checkpoint décalé et suffixe incomplet en EditMode, sans arrêt brutal du processus | testé auto et Unity partiel |
| B06 | `game/Assets/Palimpseste/Drawing` | engagement au premier trait, budget d'encre, fermeture et régions codés ; limites et fermeture partielle non couvertes exhaustivement | exécuté Unity partiel |
| B07 | `game/Assets/Palimpseste/Library` | EditMode : reprise d'événements durables au-delà du checkpoint, suffixe incomplet tronqué, événement complet altéré rejeté ; PlayMode : interruption d'un trait actif au focus et `up` durable ; aucune coupure brutale réelle ni Alt+Tab physique instrumentés | testé Unity partiel |
| B08 | `backend/Palimpseste.Api`, `backend/migrations/001_initial.sql`, `tests/api_http_smoke.py` | API loopback sous `PalRuntimeSvc`, `/health/ready` 200 après redémarrage ; invitation et capture réelles, job `ready`, paquet téléchargé. | testé auto et Player privé |
| B09 | `backend/Palimpseste.Worker/JobRepository.cs`, `tests/Palimpseste.Worker.DbSmoke` | smoke PostgreSQL isolé : claim, lease, fencing, nombre durable de réparations, tentative incertaine, incident ; panne contrôlée après A et pendant B avec description conservée, reprise et rejet des fences périmés | testé auto partiel |
| B10 | `backend/Palimpseste.Provider` | `codex exec` sous compte dédié, `gpt-5.6-luna`/`max` attestés par preuve composite v2 ; vrai job joueur A/B terminé avec deux tentatives réussies. | Luna réel et pipeline joueur exécuté |
| B11 | `game/`, `backend/Palimpseste.Api` | Premier dessin : Player BeamVisibleReady, A « Faisceau courbe de lave », fiche, labo, lancement et relecture sans API. Second dessin : capture locale en attente après lancement hors ligne ; bouton « Reconnecter » ajouté et build produit, reprise non vérifiée. | premier parcours privé exécuté ; reconnexion du second à tester |
| B12 | `shared/Palimpseste.Core/GeometryResolver.cs` | smoke fixture PNG avec trou conservé ; les A/encre de calibration produisent des chemins et un masque dans RealProbe ; le paquet joueur contient deux artefacts de géométrie et un masque, avec trajectoire PlayMode liée à l'encre | testé auto, réel et PlayMode ; cas limites ouverts |
| B13 | `backend/Palimpseste.Provider`, `backend/Palimpseste.Worker` | B réel de calibration validé sur A figée ; B du job joueur terminé, plan/`compiled_spell` publiés, paquet PlayMode 2/2 et lancement Player. | Luna B et Player exécutés |
| B14 | `shared/Palimpseste.Core/SpellCompiler.cs` | Plan réel du job joueur compilé et publié en `compiled_spell`, téléchargé et vérifié par le Player ; paquet de calibration testé séparément. | compilateur et publication joueur exécutés |
| B15 | `shared/Palimpseste.Core/SpellCompiler.cs` | borne ≥476 ticks fixture ; paquet de vérification du vrai B borné à une instance, un tick et zéro application d'effet | testé auto partiel |
| B16 | `shared/Palimpseste.Core`, `backend/Palimpseste.Storage`, `game/Assets/Palimpseste/Library` | Même sort ouvert et lancé après arrêt de l'API et redémarrage de BeamVisibleReady ; tests de corruption cache antérieurs historiques. | réutilisation sans API observée, autres postes ouverts |
| B17 | `game/Assets/Palimpseste/SpellRuntime` | casts, annulation `ResetTargets` et extinction de colliders testés en PlayMode ; saturation longue non mesurée | testé Unity partiel |
| B18 | `game/Assets/Palimpseste/SpellRuntime` | projectile guidé : touche et impulsion ; projectile courbe compilé : touche, dégâts et brûlure ; murs fins, rebond/pénétration et filtres non exhaustifs | testé Unity partiel |
| B19 | `game/Assets/Palimpseste/SpellRuntime` | Faisceau sans dégâts : paquet joueur lancé, `Lancers : 1`, `Dégâts : 0` ; correctif visibilité 0,6 s PlayMode 2/2, effet visible puis disparu dans le Player. Occlusion/chaîne non exhaustives. | testé PlayMode et Player partiel |
| B20 | `game/Assets/Palimpseste/SpellRuntime` | champ mouille une cible dans l'empreinte et épargne une petite cible dans un trou ; onde soigne un allié sans toucher l'hostile ; fronts rapides non couverts | testé Unity partiel |
| B21 | `game/Assets/Palimpseste/SpellRuntime` | barrière intercepte raycast, expire et désactive ses colliders ; piège ralentit hostile ; réarmement et interactions multiples ouverts | testé Unity partiel |
| B22 | `game/Assets/Palimpseste/SpellRuntime` | dommages, soin de 12000 milli, impulsion, brûlure, Mouillé et ralentissement affirmés en PlayMode ; cumul/réactions complets ouverts | testé Unity partiel |
| B23 | `game/Assets/Palimpseste/Scenes` | scène d'épreuve et contrôles de visée exécutés dans BeamVisibleReady ; un sort joueur réel lancé, scénarios de recette complets ouverts | testé Unity partiel |
| B24 | `game/Assets/Palimpseste/Presentation`, `SpellRuntime` | Ancien paquet de calibration Direct3D 12 2/2 ; paquet joueur PlayMode 2/2 et capture du Player BeamVisibleReady avec effet visible puis disparu. | rendu joueur exécuté, jugement humain ouvert |
| B25 | `game/Assets/Palimpseste/Presentation`, `Library` | AudioSource procédural et échantillons affirmés en PlayMode sur le paquet du joueur ; aucun créateur n'a encore écouté le son du sort. | testé technique, écoute humaine ouverte |
| B26 | `game/Assets/Palimpseste/Bootstrap`, `Scenes`, `Service/WindowsCredentialStore.cs` | UI française et parcours dessin→texte→fiche→labo vus dans BeamVisibleReady ; 1024 × 768 et 1920 × 1080 contrôlés sur des archives historiques ; coffre Windows écrit/relu/effacé sur un build précédent ; focus testé en PlayMode ; clavier/accessibilité et autres tailles non exhaustifs | exécuté Unity partiel |
| B27 | `backend/`, `ops/backup-lab.ps1`, `ops/restore-lab.ps1`, `ops/backup-runtime.ps1`, `ops/restore-runtime.ps1`, `evidence/public/backend/` | restauration à blanc labo : 4/4 artefacts DB et API GET ; scripts runtime éprouvés sur base synthétique distincte : dump, 10/10 fichiers hashés, 4 lignes DB, ACL service M ; snapshot privée de la capture Unity | testé auto partiel |
| B28 | `game/`, `backend/`, `evidence/public/unity/b28-final-endurance.md` | Archive historique SHA `343a5858…` : 662,734 s, 133 points Windows tous répondants, RSS +13 099 008 octets, privés +27 426 816 ; 13 clics périodiques, bursts 20/30 et deux resets sur fixture manuelle, deux instances actives au maximum ; aucun Profiler, FPS de présentation, coût CPU p95 ni saturation forte sur le build actuel | observation partielle sur ancien build |
| B29 | `tests/`, `evidence/` | Job joueur réel `ready`, A/B réussis 2/2, description/plan/paquet publiés, lancement Player, effet visible et relecture sans API ; corpus inédit de 30 dessins non réalisé. | parcours privé testé, recette finale ouverte |
| B30 | `docs/`, `game/Build/`, `deliverables/` | `WindowsPlayerBeamVisibleReady/` lancé/rejoué sans API. `WindowsPlayerReconnectPlayable/` construit : journal Unity `PALIMPSESTE_BUILD_OK` et retour 0 annoncé, 29 fichiers identiques au brut ; nouveau Player pas encore lancé. Nouveau ZIP non créé ; autre machine et verdict humain ouverts. | build correctif en dossier, exécution à vérifier |
| L01 | `backend/Palimpseste.Provider/CodexSettings.cs` | Effort `max` demandé et attesté par diagnostic A/B composite ; verrou technique `PALIMPSESTE_EFFORT_VERIFIED=true` pour le laboratoire privé. | effort technique attesté, verdict artistique ouvert |
| L02 | `backend/Palimpseste.Provider/CodexProcessRunner.cs` | Transport `codex exec` réel sous compte ChatGPT du service, deux appels réussis sur le job joueur, sans clé API dans le Player. | transport réel et joueur exécutés |
| L03 | `backend/Palimpseste.Provider/LunaCodexProvider.cs` | Calibration A `1.3` : deux images réelles ; job joueur distinct : A publié et affiché « Faisceau courbe de lave ». Verdict humain sur fidélité absent. | A multimodal réel et Player exécutés, fidélité ouverte |
| L04 | `backend/Palimpseste.Provider/LunaCodexProvider.cs`, `backend/Palimpseste.Worker` | B de calibration repris sur A figée ; B du job joueur terminé, plan compilé et lancé dans le Player. | B réel et intégration joueur exécutés |
| L05 | `contracts/codex`, `shared/Palimpseste.Core/ContractJson.cs` | 17 `const` des schémas de transport typés ; 61/61 contrôles documentaires ; sorties A et B réelles conformes au schéma | contrats réels testés partiellement |
| L06 | `backend/ProviderDoctor` | Doctor installé SHA `AAAAB268…96811`, local sans `production_issues` ; preuve composite v2 SHA `56F34901…3E3F` liée aux A/B/encre et compilation, worker actif. | diagnostic et verrou technique vérifiés |
| L07 | `backend/Palimpseste.Worker`, `tests/Palimpseste.Worker.DbSmoke` | leases/fencing/10 appels, au plus deux réparations durables par étape et deux reprises du processus avant lancement ; smoke DB de panne contrôlée après A et pendant B, reprise et rejet des fences périmés réussi ; panne réelle du fournisseur ou du serveur DB non exécutée | testé auto partiel |
| L08 | `backend/Palimpseste.Provider`, `ops/` | arguments séparés, environnement enfant épuré, racines contrôlées, faux transport d'injection testé ; ACE source héritable complète et refus effectif sous service de lister la racine et d'ouvrir un source en lecture/écriture ; préflight ouvre quatre sentinelles ; spec RX et fonctions exposées observées `false` ; autres enfants, changement ACL ultérieur et modèle adversarial non éprouvés | testé local partiel, isolation à accepter |
| L09 | `ops/`, `game/Assets/Palimpseste/Editor` | Unity construit par workflow développeur ; API joueur sans route de build ; worker isolé sous `PalRuntimeSvc` a traité le dessin sans modifier le code. | séparation technique observée |
| L10 | `evidence/`, `game/Build/` | Dessin propriétaire inédit → A/B Luna → paquet → Player et relecture sans API ; build BeamVisibleReady, PlayMode paquet joueur 2/2, effet visible puis disparu. Recette 30 dessins, autre machine, audio et verdicts humains ouverts. | boucle technique privée exécutée, M7 non accepté |

## Jalons et point de reprise technique

Aucun jalon n'est marqué `accepté humainement`. M0 à M6 disposent des couches techniques indiquées dans la matrice ; les tests de panne et de couverture exhaustive restent partiels. M7 dispose désormais d'une boucle privée **exécutée** sur un dessin joueur, avec build Windows et relecture après arrêt de l'API, mais la recette finale reste ouverte.

| Jalon | Réalisation technique observée | Porte encore ouverte |
|---|---|---|
| M0 | Dépôt, contrats, API health et build Unity réels | Arbitrages P01–P10 et périmètre à valider humainement |
| M1 | Dessin, raster, journal et capture réseau réels ; job propriétaire traité | Essai humain de l'engagement et panne brutale du lecteur |
| M2 | A multimodal réel publié pour le job joueur et affiché dans le Player | Verdict de fidélité sur A de calibration et sur la capture courbe |
| M3 | Géométrie, B, compilation et publication du paquet joueur réels | Comparaison humaine des clauses et règles sur corpus plus large |
| M4 | Six porteurs et effets principaux testés en PlayMode antérieur ; faisceau joueur lancé | Scénarios physiques exhaustifs et jugement humain |
| M5 | Rendu du paquet joueur PlayMode 2/2, build BeamVisibleReady, effet visible puis disparu ; audio technique antérieur | Écoute humaine du son, paires de dessins et reconnaissance visuelle |
| M6 | Service isolé, preuve composite, reprise DB, sauvegarde et cache ; même sort relu et lancé sans API | Pannes fournisseur/DB réelles, autre compte/poste et revue d'isolation plus large |
| M7 | Boucle privée dessin → Luna A/B → compilation → Player → relecture sans API exécutée | 30 dessins inédits, second créateur, machine propre, verdicts humains signés ; M7 non accepté |

**Reprendre ici.** Lancer `WindowsPlayerReconnectPlayable` avec le même profil Windows, ouvrir le second dessin local conservé, essayer « Reconnecter », puis « Transmettre » si ce bouton apparaît ; vérifier la création d'un nouveau job et la publication de A avant toute affirmation de réussite. Si la reprise échoue, conserver le journal et relever le message exact ; l'utilisateur accepte de redessiner. Conserver le premier job `ready` et son paquet téléchargé sans le relancer pour obtenir une autre lecture. Faire écouter le son et recueillir les verdicts des créateurs. Puis mener la recette inédite et les tests sur une autre machine. Inspecter les traces durables avant toute reprise d'une tentative fournisseur incertaine : Codex peut avoir consommé du quota sans réponse publiée. Garder `auth.json`, les rapports privés, la base et les identifiants hors du dépôt. Le worker lit `DATABASE_URL` depuis le coffre privé sous le compte de service. Ne pas ouvrir à plusieurs joueurs avant clarification du droit d'usage de l'abonnement individuel et des réglages de recharge.

Tests récents observés : validation/compilation hors ligne de la calibration par Doctor sans appel modèle ; sécurité du vérificateur composite réussie sur cas synthétiques ; premier job joueur A/B `success` 2/2 et `ready` ; paquet joueur PlayMode 2/2 ; build Unity BeamVisibleReady code 0 ; capture Player effet visible puis disparu ; relance et lancement du même sort sans API ; API redémarrée et `/health/ready` 200. Pour le second dessin, seul l'état local en attente et l'absence de job ont été observés ; le build correctif affiche la réussite dans le journal Unity, sans essai du nouveau Player. Les tests antérieurs (.NET Release, QA 61/61, smoke DB/API, EditMode, PlayMode et endurance) sont documentés dans les preuves de leurs builds respectifs, sans être attribués au nouveau Player.
