# Livraison Windows — objet 3D interprété

Le catalogue contient **141 recettes distinctes** composées des **24 effets primitifs** du Player. Trois jobs joueur réels ont utilisé ce catalogue et atteint `ready` ; leurs interprétations et le rendu du nouveau Player n'ont pas reçu de verdict humain. La décision [D09](../docs/DECISIONS.md) ajoute **21 formes 3D contrôlées** qui représentent l'objet imaginé par Astra. Le backend A `2.1`/B `1.8` est déployé localement et un [diagnostic A/B réel](../evidence/public/backend/semantic-visual-generation-2026-09-20.md) a compilé un golem. Voir [l'état courant](../docs/IMPLEMENTATION_STATUS.md) et [la bibliothèque](../docs/EFFECT_LIBRARY.md).

Le [ZIP Player Windows final](Palimpseste-Windows-x64-IL2CPP.zip) a été recréé le 20 septembre 2026 depuis [WindowsSemanticVfxPlayable](../game/Build/WindowsSemanticVfxPlayable/Palimpseste.exe) : **44 157 438 octets**, SHA-256 `4904A3F42D52D14683D7577C3A05829E49317B1C12572093DACE6339326464E6`. Ses **29 fichiers** totalisent **122 350 769 octets** ; chaque entrée du ZIP et chaque fichier du dossier jouable correspond par SHA-256 au build brut, sans les deux dossiers `DoNotShip`. L'EXE a pour SHA-256 `049F79454586F2AC5445F26B55191CF6611BE62F10C4A5E12F92F806050149C2` et `GameAssembly.dll` pour SHA-256 `59124E2AE6463AA29499EE1043AAD816E0D8ACAFDBB2D85F13634F7A1E73B6C1`. Le premier build sémantique, provisoire, est conservé séparément dans [la preuve](../evidence/public/unity/semantic-visuals-2026-09-20.md).

Le [ZIP backend final](Palimpseste-Backend-Windows-x64.zip) contient A `2.1`/B `1.8`, les contrats 3D et le suivi courant : **111 439 621 octets**, SHA-256 `44D25C9986B4FDA4853750DBD6922698A8D1187DE956BDDF6A393348EE15E764`. Ses **107 fichiers** ont été comparés au stage par SHA-256, sans écart ; les empreintes attendues de l'API, du worker et du script enfant de l'archive concordent. Aucun `.pdb`, `.env`, clé ou `auth.json` n'est inclus. Cette archive reste **réservée à l'opérateur** ; son installation sur un autre PC et l'ouverture à des joueurs externes ne sont pas testées.

Le dossier jouable actuel est [WindowsSemanticVfxPlayable](../game/Build/WindowsSemanticVfxPlayable/Palimpseste.exe) : conserver ensemble ses 29 fichiers. Unity 6000.3.24f1 a terminé le **nouveau** build Windows IL2CPP/URP avec `Build Finished, Result: Success`, `PALIMPSESTE_BUILD_OK` et retour 0. Le matériau d'émission `LabEmissive.mat` apparaît dans le [journal de build final expurgé](../evidence/public/unity/semantic-visuals/build-windows-release.log). Le renderer 3D a passé **5/5 tests PlayMode ciblés**, et le cache hors ligne **1/1 test EditMode** ; voir [la preuve](../evidence/public/unity/semantic-visuals-2026-09-20.md). La [capture graphique](../evidence/public/unity/semantic-forms-preview-20260920.png) provient d'une fixture, pas d'un sort joueur. Le nouveau rendu n'a pas encore reçu de verdict visuel du créateur.

La génération demande le service privé API + worker sur cette machine. Le worker exécute `codex exec` sous le compte Windows isolé `PalRuntimeSvc` : Astra reçoit l'image, Luna prépare un plan de sort déclaratif, puis le compilateur serveur le vérifie. Aucun jeton Codex, secret de base ou clé API n'est livré dans le Player. Les scripts d'administration et l'archive backend sont réservés à l'opérateur ; le worker joueur ne peut pas lancer un build Unity ou modifier le code du projet. Le laboratoire actuel utilise `127.0.0.1` ; aucun accès HTTPS public n'est livré.

Pour reprendre l'essai privé, suivre [TESTER_MAINTENANT.md](../docs/TESTER_MAINTENANT.md) et l'[état exact](../docs/IMPLEMENTATION_STATUS.md). L'API et le worker A `2.1`/B `1.8` installés sur ce PC répondent localement. Trois jobs joueur réels ont déjà abouti avec le catalogue de recettes. Le diagnostic `visual_form` a compilé un golem avec des dimensions contrôlées, mais son lancement dans le Player final n'est pas attesté.
