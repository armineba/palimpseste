# Livraison Windows en cours

Le [ZIP Player Windows actuel](Palimpseste-Windows-x64-IL2CPP.zip) a été recréé le 20 septembre 2026 à partir du dossier jouable ci-dessous : 44 045 012 octets, SHA-256 `4AF6EF5D7449CBE89094755A3B6A045D5EF75DA21706C40484403FB17B419A3F`. Les 29 fichiers extraits totalisent 122 113 753 octets et correspondent au dossier nettoyé.

Le [ZIP backend actuel](Palimpseste-Backend-Windows-x64.zip) a été régénéré le même jour : 111 355 199 octets, SHA-256 `F1A41D7E0B5DC6E0CB1348D93EAD475E4EC1BE4E405A6F380A216086173ACB27`. Il contient l'API, le worker, le doctor, la référence du dessin libre, les prompts Astra/Luna et la migration 007 ; aucun fichier de secret n'y est livré. Cette archive est **réservée à l'opérateur** : ses scripts d'administration doivent rester hors d'accès du compte worker. La régénération du ZIP ne prouve pas une installation sur un autre PC ou une ouverture à des joueurs externes.

Le dossier jouable actuel est [WindowsAppearanceVfxPlayable](../game/Build/WindowsAppearanceVfxPlayable/Palimpseste.exe) : conserver ensemble ses 29 fichiers. Unity 6000.3.24f1 a terminé le build Windows IL2CPP/URP avec le code 0. Il permet de dessiner plusieurs traits, de terminer le dessin avec un bouton, de lire la description Astra et d'ouvrir le sort compilé dans le laboratoire. Une palette visuelle contrôlée a été ajoutée ; son test PlayMode ciblé passe 1/1. Le VFX de cette version n'a pas encore été inspecté visuellement par le créateur.

La génération demande le service privé API + worker sur cette machine. Le worker exécute `codex exec` sous le compte Windows isolé `PalRuntimeSvc` : Astra reçoit l'image, Luna prépare un plan de sort déclaratif, puis le compilateur serveur le vérifie. Aucun jeton Codex, secret de base ou clé API n'est livré dans le Player. Les scripts d'administration et l'archive backend sont réservés à l'opérateur ; le worker joueur ne peut pas lancer un build Unity ou modifier le code du projet. Le laboratoire actuel utilise `127.0.0.1` ; aucun accès HTTPS public n'est livré.

Pour reprendre l'essai privé, suivre [TESTER_MAINTENANT.md](../docs/TESTER_MAINTENANT.md) et les états exacts dans [IMPLEMENTATION_STATUS.md](../docs/IMPLEMENTATION_STATUS.md). Le backend actuel a été empaqueté avec [package-backend.ps1](../ops/package-backend.ps1) ; l'API et le worker installés sur ce PC répondent localement, tandis que leur déploiement depuis ce ZIP sur une autre machine reste à tester. Aucun appel modèle réel avec les nouveaux champs de palette n'est attesté.
